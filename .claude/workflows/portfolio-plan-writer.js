export const meta = {
  name: 'portfolio-plan-writer',
  description: 'docs/ karar setinden faz başına uygulama planı yaz (writing-plans formatı), çapraz tutarlılık ve yer tutucu denetimi yap',
  phases: [
    { title: 'Plans', detail: 'Faz 0-5 için 6 paralel plan yazarı (opus)' },
    { title: 'Review', detail: 'spec kapsamı + fazlar arası arayüz tutarlılığı (opus), mekanik kontrol (haiku)' },
  ],
}

const REPO = '/Users/dogancanyildiz/Dev/DCYLDZ/portfolio'
const DOCS = REPO + '/docs'
const PLANS = DOCS + '/plans'
const SKILL = '/Users/dogancanyildiz/.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0/skills/writing-plans/SKILL.md'
const CONTENT = REPO + '/.local/content/portfolio-content.md'

const GLOBAL = `
GLOBAL KISITLAR (her plana "## Global Constraints" bölümü olarak birebir kopyalanır):
- next 16.3.3 (Active LTS), react 19.2.x, tailwindcss 4.3.x, next-intl 4.13.7, velite 0.4.0 (EXACT pin, caret yok), motion 13.1.1 (import: motion/react), node 24 (.nvmrc), package.json engines.node ">=20.9", npm (lockfile commit edilir).
- Paket yöneticisi npm; script'ler: dev, build, start, lint, typecheck (tsc --noEmit), test (vitest run), format (prettier --check).
- Test aracı: vitest (node environment) lib/ ve app/api kodu için; UI için build çıktısı ve curl tabanlı doğrulama. Faz 0 vitest'i kurar, sonraki fazlar kullanır.
- output: 'standalone'; edge runtime kullanılmaz; next/image remotePatterns TANIMLANMAZ; next/font/google KULLANILMAZ (woff2 vendor + next/font/local).
- URL şeması: EN kökte (/, /about), TR /tr altında; localeDetection kapalı; çevirisi olmayan içerik diğer dilin sitemap/hreflang'ına girmez.
- İçerik: .local/content/portfolio-content.md gerçek kaynak; "Alex Chen", example.com, alex@example.com, placeholder sosyal link ve CSS gradyan kapak yayına çıkmaz. Görseli olmayan proje kapaksız yayınlanır.
- Metin üslubu (EN ve TR site metinleri, commit mesajları, yorumlar): uzun çizgi (em dash) ve en dash kullanılmaz; kısa çizgi, virgül, iki nokta kullanılır.
- Commit mesajları Conventional Commits (feat:, fix:, chore:, refactor:, docs:), gövde İngilizce, AI atıf/co-author satırı ASLA eklenmez.
- Güvenlik: RESEND_API_KEY, CONTACT_EMAIL, FROM_EMAIL, GATUS_URL yalnızca Runtime env; NEXT_PUBLIC_SITE_URL Build env. .local/, .nodeterm/, .env* Docker build context'ine girmez. Gerçek IP CF-Connecting-IP'den, yalnızca Cloudflare IP aralıklarından gelen istekte güvenilir.
- Cloudflare proxied (turuncu bulut) + Full (strict) önde; dogancanyildiz.sh -> dogancanyildiz.com 301 Cloudflare Redirect Rule ile (tek atlama, path korunur).
- Her faz tek dal (feature/faz-N-slug), tek PR; faz bitiş kriteri planın sonunda "Bitti sayılma kriteri" olarak yer alır ve doğrulama komutları içerir.
`

const PHASES = [
  { n: 0, slug: 'guvenlik-ve-hijyen', title: 'Faz 0: Güvenlik ve hijyen', docs: ['00-ozet-ve-karar.md', '09-guvenlik.md', '01-mevcut-durum-denetimi.md', '05-backend-icerik-ve-servisler.md', '10-yol-haritasi.md'],
    brief: `Kapsam (10-yol-haritasi.md Faz 0 maddeleri): next 16.1.6 -> 16.3.3 + eslint-config-next 16.3.3, package-lock normalize; framer-motion -> motion 13.1.1 (tüm import'lar motion/react); opengraph-image.tsx edge runtime satırı silinir; next.config.ts: output standalone, poweredByHeader false, headers() ile güvenlik başlıkları + CSP (09-guvenlik.md'deki taslak); .nvmrc 24 + engines; typecheck/format/test script'leri, prettier ve vitest kurulumu (vitest.config.ts, ilk test); src/app/api/health/route.ts; contact sertleştirme: sunucu taraflı honeypot, in-memory sliding window rate limit (src/lib/rate-limit.ts, IP = CF-Connecting-IP güvenilirse, yoksa x-forwarded-for ilk değer), uzunluk sınırları, email regex, jenerik hata, prod'da zorunlu env kontrolü (src/lib/env.ts); NEXT_PUBLIC_SITE_URL .env.example'a, robots/sitemap example.com fallback'i kaldırılıp eksikse build'de throw; public/ ölü SVG'ler silinir; README Coolify/Docker için yeniden yazılır. Rate limiter, validateBody, env kontrolü için vitest testleri planda tam kodla yer alır. Renovate config (renovate.json: patch/minor automerge) bu fazda eklenir.` },
  { n: 1, slug: 'deploy-hatti', title: 'Faz 1: Deploy hattı (Docker, Coolify, Cloudflare, Traefik)', docs: ['00-ozet-ve-karar.md', '06-devops-ve-deploy.md', '09-guvenlik.md', '10-yol-haritasi.md', '11-acik-sorular.md'],
    brief: `Kapsam: çok aşamalı Dockerfile (deps -> builder -> runner, node:24-alpine, non-root node kullanıcısı, .next/standalone + .next/static + public kopyalama, CMD ["node","server.js"], HOSTNAME=0.0.0.0, PORT=3000, velite build adımı için yer bırakılır: "npm run build" içinde velite çalışacak şekilde script tasarımı Faz 4'te tamamlanır), .dockerignore (.local, .nodeterm, .git, node_modules, .next, .env*, *.md hariç README), docker-compose.yml yalnızca yerel doğrulama için; GitHub Actions .github/workflows/ci.yml (PR + push main: npm ci, lint, typecheck, test, build; image push yok); Coolify kurulum adımları (GitHub App, Dockerfile build pack, auto deploy, Preview Deployments, health check /api/health, env Build/Runtime ayrımı tablosu, coolify#7500 için start-period ve doğrulama adımı); Cloudflare: DNS kayıtları, proxied, SSL Full (strict), Redirect Rule .sh -> .com, cache rule _next/static, rate limiting rule /api/contact, Bot Fight Mode; Traefik: forwardedHeaders.trustedIPs Cloudflare aralıkları (Coolify proxy config), HSTS/compress middleware etiketleri, buffering kapatma, redirectregex yedek yolu; origin'i Cloudflare IP'lerine kısıtlama (ufw veya Traefik ipAllowList); Resend'de dogancanyildiz.com SPF/DKIM/DMARC; yerel doğrulama: docker build, docker run, curl /api/health ve /; Coolify tarafındaki adımlar el ile yapılan checklist olarak, kod adımları TDD ile. Bu faz kodu Dockerfile/CI dosyaları olduğu için testler: hadolint (npx hadolint yerine docker run hadolint), docker build başarısı, container içinden curl.` },
  { n: 2, slug: 'i18n-app-lang', title: 'Faz 2: i18n yeniden mimarisi (app/[lang] + next-intl)', docs: ['00-ozet-ve-karar.md', '04-i18n.md', '07-seo-ve-metadata.md', '01-mevcut-durum-denetimi.md', '10-yol-haritasi.md'],
    brief: `Kapsam: next-intl 4.13.7 kurulumu (src/i18n/routing.ts defineRouting locales ['en','tr'] defaultLocale 'en' localePrefix 'as-needed'; src/i18n/request.ts; src/i18n/navigation.ts createNavigation; src/proxy.ts createMiddleware localeDetection false, matcher API ve statik dosyaları dışlar); app/ yapısı app/[lang]/(layout.tsx, page.tsx, about/, projects/, projects/[slug]/, contact/) + app/api/ dışarıda; generateStaticParams her locale; setRequestLocale her page/layout'ta; messages/en.json ve messages/tr.json (mevcut translations.ts'teki anahtarlar taşınır, "Alex Chen" değerleri bu fazda geçici placeholder olarak kalabilir ama açıkça "Faz 4'te değişecek" notu düşülür); translations.ts, locale-provider.tsx, use-translation.ts silinir; cookies() çağrıları kaldırılır; generateMetadata locale param'dan, alternates.canonical + languages + x-default, metadataBase; sitemap.ts ve robots.ts iki locale; language-switcher URL tabanlı (usePathname + Link from navigation); Person JSON-LD (ana sayfa); html lang param'dan. Bitti kriteri: next build tablosunda tüm içerik route'ları ● (SSG), yalnızca /api/* ƒ; curl -I / ve /tr 200, /en 404 veya redirect (as-needed'de /en kök'e yönlenir, doğrula); head'de hreflang linkleri. Testler: routing config unit test, sitemap üretimi unit test (vitest), build çıktısı grep. Not: next/root-params Route Handler'da çalışmaz, contact route locale'i body'den alır.` },
  { n: 3, slug: 'tasarim-sistemi', title: 'Faz 3: Tasarım sistemi (tipografi, palet, layout, hareket, erişilebilirlik)', docs: ['00-ozet-ve-karar.md', '03-tasarim-ui-ux.md', '01-mevcut-durum-denetimi.md', '10-yol-haritasi.md'],
    brief: `Kapsam: fontlar: @fontsource-variable/geist, @fontsource-variable/geist-mono, @fontsource/instrument-serif paketlerinden woff2 dosyaları (latin + latin-ext) src/fonts/ altına kopyalanır (npm postinstall değil, tek seferlik script + commit), next/font/local ile üç değişken (--font-sans, --font-mono, --font-display) layout'ta body'ye; globals.css: --font-fraunces referansı --font-display'e; token blokları 03-tasarim-ui-ux.md'deki nötr hex değerlerine (oklch'e çevrilmiş) geçirilir, emerald yalnızca --primary/--ring/status; --muted-foreground ayrıştırılır; body gradyanı ve grid overlay sadeleştirilir; surface-panel gölgesindeki rgba(4,120,87,.30) kaldırılır; bileşenlerdeki doğrudan emerald referansları temizlenir; layout: header'a Radix Dialog tabanlı mobil menü (md altı), footer'a sayfa linkleri; proje listesi satır formatına (yıl · başlık · rol · stack, mono), project-card'da başlık link + ::after tıklama alanı; hareket: LazyMotion + m (motion/react), domAnimation, stagger 40ms max 4, useReducedMotion, globals.css'e prefers-reduced-motion fallback; erişilebilirlik: contact form mesajlarına role=alert/status, 24x24 hedef denetimi, solid focus ring; opengraph-image.tsx ve icon.tsx gerçek isim/unvan ve yeni paletle (DCY monogram). Testler: token dosyasında yasaklı değer grep testi (vitest ile globals.css okunup emerald ramp hex'lerinin olmadığı), font dosyalarının varlığı, build. Görsel doğrulama adımı: npm run dev + ekran görüntüsü kontrol listesi (mobil menü açılıyor, serif h1 render, dark/light).` },
  { n: 4, slug: 'icerik-ve-yayin', title: 'Faz 4: İçerik pipeline (Velite), gerçek içerik, blog ve yayın', docs: ['00-ozet-ve-karar.md', '05-backend-icerik-ve-servisler.md', '08-icerik-stratejisi.md', '07-seo-ve-metadata.md', '04-i18n.md', '10-yol-haritasi.md', '11-acik-sorular.md'],
    brief: `Kapsam: velite 0.4.0 exact (velite.config.ts: projects ve posts koleksiyonları, Zod şema: projects {title, slug, summary, role, stack[], year, links{live?,repo?}, cover?, outcome, featured?, order}, posts {title, slug, date, summary, tags[], cover?, draft?}; locale klasörden türetilir: content/projects/{en,tr}/<slug>.mdx, content/blog/{en,tr}/<slug>.mdx; rehype-slug, rehype-autolink-headings, shiki tema); package.json build: "velite && next build", dev: "velite --watch & next dev" veya velite'ın next plugin yaklaşımı; .velite/ gitignore; src/data/projects.ts kaldırılır; sayfalar Velite çıktısından beslenir (projects listesi, [slug] detay: mono künye Role/Stack/Year/Outcome + kapak varsa next/image, yoksa kapaksız); blog: app/[lang]/blog/page.tsx, blog/[slug]/page.tsx, BlogPosting JSON-LD, RSS (app/[lang]/feed.xml/route.ts), sitemap'e yalnızca var olan çeviriler; gerçek içerik: hero/about/contact metinleri .local/content/portfolio-content.md'den EN, TR çevirileri (About içinde Speaking bloğu etkinlik · konu · tarih, sertifikalar verifyUrl opsiyonel, Harp Okulu nötr satır, CEFR yok, GDG rolleri), 4-5 proje case study (Cargo Pilot, Bilet Satın Alma öncelikli, Wikonya, Hubit, GPA veya Sportlink), footer/header gerçek linkler (github.com/dogancanyildiz, linkedin.com/in/dogancanyildiz, me@dogancanyildiz.com), logo DCY, Download CV -> /cv/dogancanyildiz-cv.pdf (dosya sahibinden gelene kadar public/cv/.gitkeep ve buton koşullu render); ilk 3-4 blog yazısı TR (başlık ve özet planda örnek olarak), seçili 1 tanesi EN; tüm "Alex Chen"/example.com kalıntısı için grep testi; yayın öncesi kontrol listesi: Lighthouse, hreflang test, Search Console, contact uçtan uca, Cloudflare redirect. Testler: velite şema doğrulama testi (geçersiz frontmatter reddediliyor), içerik kalıntı grep testi, sitemap çeviri testi.` },
  { n: 5, slug: 'altyapi-vitrini-ve-olcum', title: 'Faz 5: Altyapı vitrini (Gatus status widget), Umami, bakım otomasyonu', docs: ['00-ozet-ve-karar.md', '05-backend-icerik-ve-servisler.md', '06-devops-ve-deploy.md', '09-guvenlik.md', '10-yol-haritasi.md'],
    brief: `Kapsam: Gatus container'ı Coolify'da (docker-compose kaynağı, config/gatus.yaml: yalnızca dogancanyildiz.com endpoint'i, alerting yok, storage sqlite), status.dogancanyildiz.com Cloudflare proxied; Next tarafı: src/lib/status.ts (GATUS_URL'den /api/v1/endpoints/statuses fetch, next: { revalidate: 60 }, Zod ile parse, yalnızca {name alias, up, uptime24h, lastCheck} döndürür, hata halinde null), src/components/sections/systems.tsx async Server Component + Suspense fallback ("status unavailable" nötr), ana sayfada "Systems" bölümü: site up/down, 24 saat uptime %, son deploy zamanı ve commit SHA (Dockerfile ARG GIT_SHA + BUILD_TIME -> env NEXT_PUBLIC_* değil, runtime env olarak server component okur), stack satırı; hostname/port/IP asla çıkmaz; ileride eklenebilecekler listesi 05'ten okunup yorum olarak; Umami: Coolify'da Umami + Postgres (compose), analytics.dogancanyildiz.com, script tag'i layout'ta (async, data-website-id env), CSP script-src ve connect-src güncellemesi; Renovate zaten Faz 0'da eklendi, burada Coolify auto redeploy doğrulaması; Gatus'a Umami endpoint'i de eklenir ama widget'ta gösterilmez. Testler: status.ts parse testi (örnek Gatus JSON fixture), sızıntı testi (döndürülen objede url/hostname alanı yok), CSP başlığında umami origin var testi.` },
]

const PLAN_SCHEMA = { type: 'object', properties: { path: { type: 'string' }, taskCount: { type: 'number' }, summary: { type: 'string' }, assumptions: { type: 'array', items: { type: 'string' } }, interfacesProduced: { type: 'array', items: { type: 'string' }, description: 'sonraki fazların kullanacağı dosya/fonksiyon/env adları' } }, required: ['path', 'taskCount', 'summary', 'assumptions', 'interfacesProduced'] }

phase('Plans')
log('6 faz planı paralel yazılıyor')
const plans = await parallel(PHASES.map(p => () => agent(`Sen bir uygulama planı yazarısın. Önce ${SKILL} dosyasını oku ve formatına (header, Global Constraints, Task yapısı, Interfaces bloğu, bite-sized adımlar, No Placeholders, Self-Review) birebir uy. Sonra şu dokümanları oku: ${p.docs.map(d => DOCS + '/' + d).join(', ')}. Gerekli yerlerde repoyu incele (${REPO}/src, package.json, next.config.ts, globals.css, mevcut bileşenler) ve dosya:satır referansı ver. Gerçek içerik kaynağı: ${CONTENT} (oku, ama planın içine yalnızca gerekli alıntıları koy). Güncel API'ler için gerekirse ToolSearch ile context7 (resolve-library-id, query-docs) yükleyip next-intl / velite / motion / next 16 dokümanına bak; eğitim verisine güvenme.

${GLOBAL}

GÖREV: ${PLANS}/2026-08-27-faz-${p.n}-${p.slug}.md dosyasını yaz. Plan başlığı: "${p.title} Implementation Plan". Spec alanı: ilgili docs/ dosyalarının yolları. Dil: plan metni Türkçe, kod ve commit mesajları İngilizce. Em dash / en dash KULLANMA.
${p.brief}

Kurallar:
- Her task: Files (Create/Modify/Test, tam yol), Interfaces (Consumes/Produces, tam imza), adımlar checkbox ile, her kod adımı gerçek kodla (TypeScript/Dockerfile/YAML/JSON tam içerik), test adımı gerçek vitest kodu veya gerçek shell komutu + beklenen çıktı, commit adımı gerçek komut ve mesaj.
- "TBD", "uygun şekilde", "benzer şekilde" gibi yer tutucu YASAK. Bir önceki task'ın kodunu referanslamak yerine gereken kadarını tekrar yaz.
- Kod gerçek API'lere uymalı: next-intl 4 (defineRouting, createNavigation, getRequestConfig, setRequestLocale, hasLocale), Next 16 (proxy.ts, params Promise, generateStaticParams), velite 0.4 (defineConfig, defineCollection, s.*), motion 13 (LazyMotion, domAnimation, m, useReducedMotion).
- Planın sonunda "Bitti sayılma kriteri" bölümü: çalıştırılacak komutlar ve beklenen çıktı; ve "Devir notu şablonu" bölümü: sonraki faz ajanına aktarılacak alanlar (yapıldı / doğrulandı / açık kaldı / üretilen arayüzler).
- Planı yazdıktan sonra Self-Review yap (spec kapsamı, yer tutucu taraması, tip tutarlılığı) ve düzelt.
Dön: path, task sayısı, 2 cümle özet, varsayımlar, sonraki fazların kullanacağı arayüz adları.`,
  { label: `plan:faz-${p.n}`, phase: 'Plans', schema: PLAN_SCHEMA, model: 'opus', effort: 'high' })))

const ok = plans.filter(Boolean)
log(`${ok.length}/6 plan yazıldı`)

phase('Review')
const REVIEW_SCHEMA = { type: 'object', properties: { fixed: { type: 'array', items: { type: 'string' } }, remaining: { type: 'array', items: { type: 'string' } } }, required: ['fixed', 'remaining'] }
const review = await agent(`Sen plan denetçisisin. ${SKILL} dosyasını oku (özellikle No Placeholders ve Self-Review). Sonra ${PLANS}/ altındaki 6 plan dosyasını ve ${DOCS}/00-ozet-ve-karar.md, 10-yol-haritasi.md dosyalarını oku. Planların yazarlarının bildirdiği arayüzler: ${JSON.stringify(ok.map(p => ({ path: p.path, produces: p.interfacesProduced, assumptions: p.assumptions })))}.
Kontrol et ve DOĞRUDAN DÜZELT (Edit):
1. Fazlar arası arayüz tutarlılığı: bir fazın ürettiği dosya/fonksiyon/env/script adı sonraki fazda aynı adla mı tüketiliyor (ör. src/lib/env.ts, src/lib/rate-limit.ts, src/i18n/routing.ts, messages/*.json, velite koleksiyon adları, package.json script adları, Dockerfile ARG adları, GATUS_URL, /api/health). Uyumsuzluğu sonraki fazda düzelt, ilk tanımı koru.
2. Spec kapsamı: 10-yol-haritasi.md'deki her faz maddesinin plandaki bir task'ta karşılığı var mı; yoksa task ekle (gerçek kodla).
3. Yer tutucu taraması: "TBD", "TODO", "uygun", "benzer şekilde", "gerekirse ekle", boş kod bloğu; hepsini gerçek içerikle değiştir.
4. Global Constraints bölümü her planda var ve birebir aynı mı.
5. Em dash / en dash: grep -rn "—\\|–" ${PLANS}/ sıfır sonuç vermeli; düzelt.
6. Commit mesajlarında co-author veya AI atfı yok.
Düzeltemediklerini "remaining" olarak döndür.`,
  { label: 'review:plans', phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })

const mech = await agent(`Mekanik kontrol: ${PLANS}/ altındaki tüm .md dosyalarında (1) ilk satır "# ... Implementation Plan" ile başlıyor mu, (2) "> **For agentic workers:**" satırı var mı, (3) "## Global Constraints" başlığı var mı, (4) en az bir "- [ ] **Step" satırı var mı, (5) "Bitti sayılma kriteri" ve "Devir notu" başlıkları var mı, (6) em/en dash var mı (grep). Eksikleri Edit ile düzelt (başlık ekle, dash'i kısa çizgiye çevir). Dosya başına tek satır sonuç döndür.`,
  { label: 'check:mechanical', phase: 'Review', schema: { type: 'object', properties: { results: { type: 'array', items: { type: 'string' } } }, required: ['results'] }, model: 'haiku' })

return { plans: ok, review, mech }
