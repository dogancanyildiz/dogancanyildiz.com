# Faz 3 devir notu: tasarım sistemi (tipografi, palet, layout, hareket, erişilebilirlik)

Tarih: 2026-08-27 · Dal: `feature/faz-3-tasarim-sistemi` · HEAD: bu commit (`a9a8705` üzerine) · Base: `feature/faz-2-i18n-app-lang` @ `d8c9ddc` · Plan: `docs/plans/2026-08-27-faz-3-tasarim-sistemi.md` · Ara not: `docs/plans/handoffs/faz-3-notlar.md` (yalnızca Task 0)

Durum: kod tarafı tamam ve yerelde doğrulandı. Dalda 23 commit var: Task 0'ın plan düzeltmesi, dokuz task commit'i (Task 1-9), sekiz inceleme düzeltmesi (Task 2, 3, 8, 9 ve Task 10'un iki bloklayan bulgusu), entegrasyon turunun dört commit'i ve bu not. Bitti sayılma kriterlerinin onu da yerelde geçti (4 ve 9'un tarayıcı isteyen yarıları ve Task 10 Step 4'ün 14 maddelik ekran görüntüsü listesi hariç; bu oturuma bağlı bir tarayıcı yoktu, hepsi `docs/plans/handoffs/faz-3-manual-checklist.md` içinde sahibini bekliyor). Dal push edilmedi, PR açılmadı, CI hiç koşmadı. Faz 0 (#2), Faz 1 ve Faz 2 PR'ları da merge edilmediği için bu dal dört fazlık bir yığının tepesinde: `main` -> Faz 0 -> Faz 1 -> Faz 2 -> Faz 3.

## Süreç notu

- On bir task (0-10) tek çalışma ağacında sırayla, her biri ayrı bir alt ajanla yürütüldü; her task'tan sonra bağımsız bir inceleme (opus) koştu. Bloklayan bulgu çıkan altı task'ta (0, 2, 3, 8, 9, 10) düzeltme commit'i atıldı, tabloda `fix` satırları. Bloklayan bulguların ikisi yine Prettier'dı (Task 3 ve 9); `npm run format` hâlâ CI'da koşmuyor, bkz. "Açık kalanlar".
- Task 8 sırasında aynı çalışma ağacında çalışan başka bir süreç, uygulayıcının henüz commit'lenmemiş `contact-form.tsx` düzenlemesini geri aldı ve untracked test dosyasını sildi; uygulayıcı `git status` ile fark edip yeniden uyguladı, sonuç kayıpsız. Worktree izolasyonu olmadan paralel ajan çalıştırmanın riski olarak kaydediliyor.
- Entegrasyon turunda (bu not) inceleme bulgularından dördü küçük, testli ve bu fazın işi olduğu için kapatıldı: skip link'in odakta kaybolan padding'i ve `<main>`'in odaklanamaması, tema anahtarının İngilizce sabit etiketi, dark modda ters dönen panel gölgesi, Faz 0'dan devredilen Tailwind 4.2 -> 4.3 güncellemesi. Sonuncusu, planın hiç görmediği bir kaynak tarama sorununu ortaya çıkardı; ayrıntı "Entegrasyon turu" bölümünde.
- Bu oturumda yeni alt ajan açılmadı, fork tabanlı skill çağrılmadı.
- Doğrulama portları: Task 5 dev sunucusu 3161, entegrasyonda `next start` 3171, Docker container'ı 3172 (plan 3000 yazar; yerel çakışmadan kaçınıldı). Doğrulama sonunda `faz3-verify` container'ı ve `portfolio-local:faz3` imajı silindi.
- Plan dosyasındaki 137 checkbox'ın hiçbiri işaretlenmedi; Faz 0-2 planlarında da hiç işaretlenmemiş, yani sürecin sözleşmesi bu. İki inceleme bunu not düştü, davranış değiştirilmedi.

## Yapılanlar

| Commit | Task | Özet |
| --- | --- | --- |
| `21895d0` | Task 0 | Plan doğrulaması iki hata buldu ve düzeltti: OG route'u kökte değil `src/app/[lang]/opengraph-image.tsx`'te ve locale bağımlı (Task 9'un hedefi, kodu, testi ve kriter 9 güncellendi); `src/app/global-not-found.tsx` planda hiç yoktu (Task 2 ve 4'ün Files listesine girdi). `tests/plans/faz-3-plan.test.ts` iki düzeltmeyi kilitliyor, `faz-3-notlar.md` ara notu |
| `32e937c` | Task 1 | `scripts/vendor-fonts.mjs` + `npm run vendor:fonts`: üç `@fontsource*` paketinden (5.3.0 exact pin, devDependency) altı woff2 `src/fonts/` altına, iki woff `public/fonts/og/` altına, üç lisans dosyası; `src/fonts/index.ts` altı `next/font/local` yükleyicisi (latin ve latin-ext ayrı `unicode-range`), `tests/fonts.test.ts` |
| `aef6e7c` | Task 2 | `fontVariables` layout'a ve `global-not-found.tsx`'e bağlandı; `globals.css` `--font-*-stack` zinciri vendor yüzlerle başlıyor, ölü `--font-fraunces` gitti, h1 serif display, h2-h4 Geist Sans, `.pull-quote`; `tests/design-tokens.test.ts`; Turbopack için `unicode-range` literalleri inline edildi, `page-metadata.test.ts`'e `next/font/local` mock'u |
| `153ccc9` | Task 2 fix | İnceleme: yüzler hiçbir elemana uygulanmıyordu. Font sınıfları `<body>`'den `<html>`'e taşındı (iki dokümanda da), testler davranışa bağlandı |
| `08c8306` | Task 3 | `:root` / `.dark` token blokları 03-tasarim-ui-ux.md paletinin oklch karşılığına geçti; emerald yalnızca `--primary`, `--ring`, `--status-up`; `--status-down` ve `--destructive-foreground` yeni; `@theme inline`'da `--color-status-up/down` |
| `0343c45` | Task 3 fix | Planın altıncı testi (solid focus ring, alfa yok) yazılmamıştı, eklendi |
| `62285f1` | Task 3 fix | `globals.css` ve test Prettier'a uyduruldu |
| `4fba5c5` | Task 4 | Body'nin üç katmanlı gradyanı ve grid overlay'i tek soluk hairline grid'e indi; `.surface-panel` ve `Card` gölgesi `color-mix` ile nötr; bileşenlerdeki `rgba()`, `radial-gradient`, `bg-accent/*`, `border-primary/*`, `hover:-translate-y-*` temizlendi (header, language-switcher, contact-form, hero, project-card, project-detail, button, card, input, textarea); `collectTsxFiles` taraması `rgba(` / `radial-gradient` / emerald hex'i `src/**/*.ts(x)` için yasaklıyor |
| `92fbe6f` | Task 5 | `src/lib/nav.ts` (`navItems`), `mobile-menu.tsx` (Radix `Dialog`, `md` altı), header ve footer tam yeniden yazım (footer'da "Pages" bloğu, sabit mailto yerine `contact.email`), `.tap-target`, `nav.menu/openMenu/closeMenu`, `footer.navTitle`, `a11y.skipToContent`, `tests/nav.test.ts` |
| `60c9605` | Task 6 | `src/lib/motion.ts` (`STAGGER_SECONDS` 0.04, `MAX_STAGGER_ITEMS` 4, `fadeUp`/`staggerContainer`/`staggerItem`), `motion-provider.tsx` (`LazyMotion` + `domAnimation` + `strict`), on bileşen `motion/react-m` + `useReducedMotion`'a geçti, `whileInView` ve sınırsız `index * delay` gitti, `prefers-reduced-motion` CSS fallback'i, `<main id="main">`, `tests/motion.test.ts` |
| `04d5bd2` | Task 7 | `project-row.tsx` (yıl, başlık, rol, sağda mono stack; `after:absolute` tıklama alanı), `projects-section.tsx` `<ul>` içinde satırlar, `project-card.tsx` artık kartı `Link` ile sarmıyor (başlık link, `::after` overlay) |
| `5295f98` | Task 8 | Contact formunda `role="alert"` / `role="status"` + `aria-busy`, `:focus-visible` solid 2px halka 2px offset, `outline-ring/50` gitti, `.skip-link` + layout'ta skip link, `tests/accessibility.test.ts` |
| `c355a50` | Task 8 fix | İnceleme: `button`/`input`/`textarea`'daki `outline-none` yeni halkayı aynı `@layer utilities` geçişinde sıfırlıyordu, kaldırıldı ve testle kilitlendi |
| `933fa71` | Task 9 | `[lang]/opengraph-image.tsx` ve `icon.tsx` gerçek kimlikle (`siteConfig.person`): DCY rozeti, yeşil nokta, domain, isim serif, unvan locale'e göre, "Konya, Türkiye"; `tests/og-image.test.ts` |
| `377ec06` | Task 9 fix | İnceleme: `ğ ş İ` OG'de Geist'e düşüyordu; `Instrument Serif Ext` yüzü kendi aile adını aldı, Prettier düzeltildi |
| `fc3e1f0` | Task 9 fix | İnceleme: `GET /icon` 404 dönüyordu; proxy matcher'ın negatif lookahead'ine `icon` eklendi (`src/proxy.ts` Task 9'un listesinde değildi) |
| `1d5be08` | Task 10 fix | İnceleme: `src/app/favicon.ico` hâlâ create-next-app üçgeniydi ve `<head>`'de `/icon`'un önüne geçiyordu, silindi |
| `598608e` | Task 10 fix | İnceleme (Faz 0'dan devir): `theme-toggle.tsx` `theme` yerine `resolvedTheme` okuyor; sistem teması dark olan ziyaretçide ilk tıklama artık boş geçmiyor |
| `9ff17df` | entegrasyon | Skip link görünür olunca `not-sr-only` padding'i sıfırlıyordu, focus kuralı `padding: 0.5rem 1rem`'i yeniden yazıyor; `<main id="main" tabIndex={-1} focus-visible:outline-none>` |
| `5e9f896` | entegrasyon | Tema anahtarının `aria-label`'ı `a11y.toggleTheme`'den (EN "Toggle theme", TR "Temayı değiştir"), iki mount durumunda da; `nav.test.ts` `a11y` paritesini kilitliyor |
| `57bf733` | entegrasyon | `--shadow-color` token'ı (`:root` foreground mürekkebi, `.dark` siyah); `.surface-panel` ve `Card` gölgesi artık `--foreground`'dan değil bundan karışıyor, dark modda açık renkli hale sorunu bitti |
| `a9a8705` | entegrasyon | `tailwindcss`, `@tailwindcss/postcss`, `@tailwindcss/node`, `@tailwindcss/oxide` 4.2.0 -> 4.3.3 (global kısıt 4.3.x); `@import "tailwindcss" source("../")` ile kaynak taraması `src/` ile sınırlandı, derlenmiş CSS 61.9 KB -> 47.6 KB |

Plan şablonundaki özet, gerçek değerlerle:

- Font vendoring: `src/fonts/*.woff2` (6 dosya: geist, geist-mono, instrument-serif × latin, latin-ext), `public/fonts/og/*.woff` (2 dosya), `src/fonts/LICENSE-*.txt` (3), `scripts/vendor-fonts.mjs`, `npm run vendor:fonts` (yeniden koşunca byte'ı byte'ına aynı çıktı, `git status` temiz).
- `src/fonts/index.ts`: altı `next/font/local` yükleyicisi, subset başına `unicode-range`, üç latin yüzü `preload: true`; `fontVariables` `<html className>`'de (layout ve `global-not-found.tsx`).
- `globals.css`: nötr `:root` / `.dark` blokları (`#f9fafb` / `#0a0c0f` zemin, `#007041` / `#4fcc8d` accent, `#60656b` / `#999fa6` muted; incelemede oklch -> sRGB çevrimi ve WCAG kontrastları plandaki tabloyla birebir doğrulandı), `--status-up` / `--status-down`, `--shadow-color`, sade body zemini (tek hairline grid, `mask-image` ile üstte sönüyor), solid focus ring, `prefers-reduced-motion` fallback, `.tap-target`, `.skip-link`, `.pull-quote`, `.eyebrow` (mono), `.surface-panel` (nötr gölge).
- `src/lib/nav.ts`, `src/components/layout/mobile-menu.tsx`, yeniden yazılan `header.tsx` ve `footer.tsx`.
- `src/lib/motion.ts`, `src/components/motion-provider.tsx`; `src/` altında `motion/react`'ten `motion` importu yok, on bileşen `m` + `useReducedMotion`.
- `src/components/sections/project-row.tsx`, satır tabanlı `projects-section.tsx`, `::after` tıklama alanlı `project-card.tsx`.
- `role="alert"` / `role="status"` + `aria-busy`, skip link (`<main tabIndex={-1}>` hedefli), 24x24 denetim tablosu (plan satır 2609-2622, repoya yazılmadı, PR gövdesine kopyalanacak; değerler kodla karşılaştırıldı ve tutuyor).
- `src/app/[lang]/opengraph-image.tsx` ve `src/app/icon.tsx` gerçek isim, unvan ve DCY monogramıyla; `favicon.ico` silindi.

## Plandan sapmalar

1. **Dal `main`'den değil Faz 2 dalından açıldı** (Task 0). Plan Step 1 `git checkout main; git pull --ff-only` diyor ama Faz 0/1/2 PR'ları merge edilmediği için dal `feature/faz-2-i18n-app-lang` @ `d8c9ddc` üzerine açıldı (`git merge-base` doğruluyor). Plan Step 2'nin `grep -c createNavigation` beklentisi 1, gerçek 2 (import + çağrı); kod doğru.
2. **Planın devralınan arayüz tablosunda iki hata** (`21895d0`): OG route'u ve `global-not-found.tsx`. Ayrıntı `faz-3-notlar.md`. Plan dosyası bu dalda değiştirildi; `tests/plans/faz-3-plan.test.ts` (13 test) düzeltmeyi kilitliyor.
3. **`vitest.config.ts` yok, `vitest.config.mts` var** (Faz 2 sapma 1 ile aynı); `tests/**/*.test.ts` deseni yeni dosyaları topluyor, ek ayar gerekmedi.
4. **Font byte sayıları plandan birkaç byte farklı** (Task 1, ör. `geist-latin.woff2` 29400 / plan 29404); plan bunu önemsiz saymıştı. `scripts/vendor-fonts.mjs` Prettier ile yeniden sarıldı, davranış aynı.
5. **`next/font/local` bildirimlerinde paylaşılan const kullanılamıyor** (Task 2, `src/fonts/index.ts`). Turbopack `localFont()` argümanlarını statik çıkarıyor ve tanımlayıcı referansını düşürüyor; gerçek hata metni `Font loader values must be explicitly written literals.` (kaynak yorumunda "missing field value" yazıyor, düzeltilmedi). `unicode-range` dizgileri altı çağrıya inline edildi. `tests/seo/page-metadata.test.ts` layout'u import ettiği için `vi.mock("next/font/local")` eklendi.
6. **Derlenmiş CSS `.next/static/css/` değil `.next/static/chunks/*.css`** (Task 2 Step 7, Task 10). Doğru yolla kontrol edildi: altı `@font-face`, iki farklı `unicode-range`.
7. **Font sınıfları `<body>`'den `<html>`'e** (`153ccc9`). Plan `fontVariables`'ı body'ye koyuyordu; `--font-*-stack` `:root`'ta tanımlı olduğu için `--font-sans-latin` vb. body'de tanımlanınca `:root` seviyesindeki stack onları görmüyordu, yüzler hiç uygulanmıyordu. İnceleme yakaladı.
8. **`.surface-panel` gölgesi Task 4 yerine Task 3'te silindi** (kapsam sızıntısı). Task 4 bu yüzden bulunmayan bir satırı değiştirmek yerine planın hedef bloğunu doğrudan yazdı; son durum plan metniyle aynı. Task 3'ün commit mesajı plandakinden farklı (`fix(design tokens): ...`, scope'ta boşluk), kural ihlali yok.
9. **`--destructive-foreground` tanımlı ama tüketilmiyor** (Task 3): iki blokta var, `@theme inline`'da `--color-destructive-foreground` yok, `src/` altında referans yok. Plan bloğu böyle istedi; ölü token olarak duruyor.
10. **Task 4'ün dosya satır numaraları ve `language-switcher.tsx` açıklaması eskiydi**: dosyada `rgba()` hiç yoktu (Faz 2'de yeniden yazılmıştı), yalnızca `bg-accent/70 -> bg-muted` uygulandı. `button.tsx` `destructive` varyantındaki `hover:-translate-y-0.5` de planın "hover'da transform yok" gerekçesiyle kaldırıldı (planda adıyla geçmiyordu).
11. **Dark modda gölge ters dönüyordu** (Task 4 incelemesi, planın kendi dizgisi). `color-mix(... var(--foreground) 35% ...)` `.dark`'ta beyaza yakın hale üretiyordu; entegrasyonda `--shadow-color` ile kapatıldı (`57bf733`).
12. **Task 5 plan dışı test ekledi** (`tests/nav.test.ts`, 13 test); Step 8'in tarayıcı yarısı (390px, focus trap, `Escape`) yalnızca curl kısmıyla koşuldu. Plan kodu birebir olduğu için header'daki marka adı `font-display` sınıfını kaybetti ve header `aria-label` (`nav.menu` = "Menu") dialog başlığıyla aynı; ikisi de sahibinin kararında.
13. **Task 6 planın listelemediği üç dosyayı taşıdı** (`about-content.tsx`, `contact-page-content.tsx`, `project-card.tsx`): üçü de eager `motion` import ediyordu ve strict `LazyMotion` altında çalışma zamanında patlardı; `project-card.tsx` spec'in örnek verdiği sınırsız `index * 0.08` gecikmesini taşıyordu. Step 12'nin `language-switcher.tsx` değişikliği ve Step 9/10'un `next/link` importu düzeltmeleri gereksizdi (Faz 2'de zaten yapılmış). `MotionProvider` plandaki gibi `ThemeProvider`'ın hemen altında değil `NextIntlClientProvider`'ın içinde (Faz 2'nin eklediği katman, işlev aynı). Commit mesajı plan metninden farklı.
14. **Task 7'nin "46 test" beklentisi eski taslaktan kalma**; gerçek toplam o an 338'di. Plan yeni test istemiyordu, TDD gereği `design-tokens.test.ts`'e "project list layout" bloğu eklendi.
15. **Task 8**: commit mesajı `--amend` ile plandaki metne çevrildi (push edilmemiş özel dal); Step 7 denetim tablosu repoya yazılmadı (PR gövdesi içeriği); `outline-none` çakışması incelemede çıktı (`c355a50`). Eşzamanlılık olayı "Süreç notu"nda.
16. **Task 9**: `Instrument Serif` latin-ext yüzü `next/og` (satori) altında `unicode-range` bilmediği için aynı aile adıyla yüklenince kullanılmıyordu; ext yüz `Instrument Serif Ext` adını aldı (`377ec06`). `/icon` proxy'de 404'tü, `src/proxy.ts` Task 9'un listesi dışında düzeltildi (`fc3e1f0`). `metadata.ogAlt` şablon değerinde kaldı ("Portfolio" / "Portfolyo"), commit gövdesindeki "alt copy changed" iddiası yanlış. JSX'te ülke adı sabit ("Türkiye"), `siteConfig.person.location.country` kullanılmıyor.
17. **Task 10 Step 4 (14 ekran görüntüsü) ve Step 7 (push + PR) koşulmadı.** Tarayıcı bağlı değildi (`list_connected_browsers` boş); PR açmak bu oturumun kuralı gereği sahibinin. Step 8'in devir notu bu dosya.
18. **Bitti kriteri 4 `npm run dev` yerine `next start` ile koşuldu** (3171). Üretim HTML'i aynı çıktıyı veriyor (`1` ve `1`). Kriter 7'nin beklediği `oklch(0.7591 0.1444 158.0)` dosyada `158` olarak duruyor (Prettier `.0`'ı düşürüyor), değer aynı.
19. **Entegrasyon plan dışı dört commit attı** (`9ff17df`, `5e9f896`, `57bf733`, `a9a8705`); gerekçeler commit gövdelerinde ve "Entegrasyon turu"nda. Tailwind güncellemesi `package.json` aralıklarını değiştirmedi, yalnızca lockfile.
20. **Testler çoğunlukla kaynak metni grep'i.** Bu fazın yedi yeni test dosyasının tamamı kaynak dizgisi iddiası; dialog'un açıldığını, `::after` alanının tıklanabildiğini, reduced motion'da animasyonun durduğunu hiçbiri kanıtlamıyor. Faz 2 ile aynı sınır; DOM tabanlı bir test kurulumu "Açık kalanlar"da.

## Doğrulananlar

Kapılar (bu dalın HEAD'inde, `NEXT_PUBLIC_SITE_URL` `.env.local`'dan `https://dogancanyildiz.sh`):

| Komut | Sonuç |
| --- | --- |
| `npm run typecheck` | exit 0 |
| `npm run lint` | exit 0 |
| `npm run format` | exit 0, `All matched files use Prettier code style!` |
| `npm test` | exit 0, `Test Files 27 passed (27)`, `Tests 367 passed (367)` |
| `rm -rf .next && npm run build` | exit 0; route tablosunda `ƒ` yalnızca `/api/contact` ve `/api/health`, `● /[lang]/opengraph-image/[__metadata_id__]` SSG, `○ /icon`, `/robots.txt`, `/sitemap.xml`, `/_not-found` |
| `npm run verify:routes` | exit 0, `Static route check passed: 20 content routes prerendered (6 project pages per locale).` |

Test dosyası başına sayılar: `tests/fonts.test.ts` 11, `tests/design-tokens.test.ts` 22, `tests/motion.test.ts` 9, `tests/nav.test.ts` 13, `tests/accessibility.test.ts` 14, `tests/og-image.test.ts` 11, `tests/plans/faz-3-plan.test.ts` 13 (bu fazın yedi yeni dosyası, 93 test); Mevcut dosyalardan `tests/i18n/app-shell.test.ts` (66) bu fazda üç test aldı (font className'i `<html>`'de, 404 dokümanında aynı className, proxy'nin `/icon` istisnası); `tests/seo/page-metadata.test.ts` (63) yalnızca `next/font/local` mock'u aldı, test sayısı değişmedi. Faz 2 sonunda 20 dosya / 271 test vardı; 271 + 93 + 3 = 367.

Bitti sayılma kriterleri (2, 4, 9 için `next start -p 3171`; `next start` standalone uyarısı basıyor, Faz 2'deki gibi doğrulama için yeterli):

| # | Kriter | Çıktı | Geçti mi |
| --- | --- | --- | --- |
| 1 | Kod kapıları | beşi de exit 0; 27 dosya, 367 test (plan en az 5 dosya / 45 test istiyor) | evet |
| 2 | Google Fonts isteği yok | `/` ve `/tr` HTML'inde üç preload: `/_next/static/media/geist_latin.p.20f3xrkj613ah.woff2`, `geist_mono_latin.p.1t7tphccnfzmb.woff2`, `instrument_serif_latin.p.3_vslfxq_--4i.woff2`; `fonts.googleapis.com\|fonts.gstatic.com` sayısı 0 | evet |
| 3 | Vendor dosyaları | `ls src/fonts/*.woff2` 6, `ls public/fonts/og/*.woff` 2 | evet |
| 4 | Mobil menü | `aria-label="Open menu"` 1 (`/`), `aria-label="Menüyü aç"` 1 (`/tr`); 390px tarayıcı kontrolü manuel listede | evet (curl yarısı) |
| 5 | reduced-motion | `@media (prefers-reduced-motion: reduce)` 1; `useReducedMotion` 10 dosyada (`hero`, `skills-strip`, `featured-projects`, `about-content`, `contact-page-content`, `contact-form`, `project-card`, `project-detail`, `project-row`, `projects-section`) | evet, SSR notu "Açık kalanlar"da |
| 6 | Palet artıkları | `grep -rn emerald src/` boş exit 1; `grep -rn "rgba(" src/` boş exit 1; `grep -c "oklch(0.516 0.114 157.2)"` 0 exit 1 | evet |
| 7 | `--primary` ve `--muted-foreground` ayrı | light `oklch(0.4794 0.1156 156.3)` / `oklch(0.5044 0.0114 252.9)`, dark `oklch(0.7591 0.1444 158)` / `oklch(0.6998 0.0123 252.1)` | evet |
| 8 | Route'lar statik | `grep -E "ƒ /"` yalnızca `├ ƒ /api/contact` ve `├ ƒ /api/health` | evet |
| 9 | OG ve favicon | `/opengraph-image/default` `200 image/png` 47761 B, `/tr/opengraph-image/default` `200 image/png` 47802 B, `/icon` `200 image/png` 560 B; PNG'ler açılıp bakıldı: "Doğan Can Yıldız" Instrument Serif'te, `ğ` ve `ı` doğru, TR unvanda `ş` ve `ü` serif, "Konya, Türkiye", DCY rozeti, yeşil nokta, `dogancanyildiz.sh` mono; icon DCY monogramı | evet |
| 10 | Uzun/en çizgi | `grep -rn` `src/` ve `messages/` boş, exit 1 (plan dosyası dahil Task 10 Step 6 da `OK`) | evet |

Task 10 Step 5 tripwire'ları: `no emerald reference: OK`, `no rgba literal: OK`, `no dead font token: OK`, `no google fonts: OK`, `no legacy motion: OK`, `old shared token gone: OK` (grep -c `0` basıp exit 1 döndüğü için `OK` satırı da geliyor).

Kriterlerin ötesinde koşulan doğrulamalar:

- Docker (CI'ın ikinci job'ı yerelde, Faz 1 hattı; Dockerfile bu fazda değişmedi): `docker run --rm -i hadolint/hadolint:v2.15.1-alpine hadolint --failure-threshold warning - < Dockerfile` -> yalnızca `DL3066 info` (Faz 1 ve 2 ile aynı), exit 0. `docker build --build-arg NEXT_PUBLIC_SITE_URL=http://localhost:3172 -t portfolio-local:faz3 .` exit 0. Container (`-p 3172:3000`, `CONTACT_EMAIL` ve `FROM_EMAIL` ile): `/api/health` `{"status":"ok",...}`; `/tr` HTML'inden çıkarılan üç woff2 yolu container'dan `200 font/woff2` ile 29400, 23128 ve 21032 byte döndü, Google Fonts referansı 0; `/opengraph-image/default` ve `/tr/opengraph-image/default` `200 image/png` (47761 / 47802 B, `next start` ile aynı), `/icon` `200 image/png` 560 B; `aria-label="Menüyü aç"` 1; `<html lang="en">` / `<html lang="tr">`; `/en` `307 -> /`, `/nope` 404; imajda `/app/public/fonts/og` iki woff, `/app/.next/static/media` altı woff2; `id -un` `node`. Container ve imaj silindi.
- Derlenmiş CSS (`.next/static/chunks/*.css`): 47644 B; altı `@font-face`, iki `unicode-range` kümesi (`U+??,U+131,...` ve `U+100-2BA,...`); `--shadow-color` iki temada (`#14181c` / `#000` ve lab karşılıkları); `.skip-link:focus-visible` `padding:.5rem 1rem` ile derleniyor; `rgba(4,120,87` / `#0478574d` yok; `outline-ring/50` tek artık, `shadcn/tailwind.css` importundan geliyor, tarama kaynaklı değil.
- Tailwind tarama etkisi (ayrıntı "Entegrasyon turu"): 4.2.0 build'i 61907 B, 4.3.3 kapsamsız 62649 B, `source("../")` ile 47644 B. Kaldırılan sınıfların hiçbiri `src/` altında kullanılmıyor (27 örnek sınıf tek tek grep'lendi).
- Çalışan sunucuda (3171): skip link iki dilde body'nin ilk elemanı (`Skip to content` / `İçeriğe geç`), `<main id="main" tabindex="-1" class="min-h-[calc(100vh-7rem)] focus-visible:outline-none">`, tema anahtarı `aria-label="Toggle theme"` (`/`) ve `aria-label="Temayı değiştir"` (`/tr`) birer kez, `<head>`'de tek favicon linki (`/icon?<hash>` 32x32), `og:image` `/`'da `https://dogancanyildiz.sh/opengraph-image/default`, `/tr/about`'ta `.../tr/opengraph-image/default`; 13 route 200, `/nope` ve `/tr/nope` 404, `/en` `307 -> /`.
- `npm run vendor:fonts` yeniden koşunca `git status` temiz (Task 10 incelemesi), sekiz dosya gerçek wOF2/wOFF magic byte'ı taşıyor; latin dosyalarında `ç ö ü ı`, ext dosyalarında `ğ İ ş` glifleri fontTools ile doğrulandı (Task 1 incelemesi).
- Kural denetimi: `feature/faz-2-i18n-app-lang..HEAD` aralığındaki hiçbir commit'te `Co-Authored-By` veya AI atfı yok; commit mesajlarında ve eklenen satırlarda uzun/en çizgi yok; `.env*`, `.local/`, `.nodeterm/` hiçbir commit'te yok; hiçbir task `git add -A` kullanmadı; sır yazılmadı.

## Entegrasyon turu

Dört commit, hepsi inceleme bulgusundan:

- **`9ff17df` skip link ve `<main>`.** Task 8 incelemesi Chrome'da ölçmüştü: odaklanınca padding 0, pill 100x22 px, metin kenara yapışık. `not-sr-only` `padding: 0` yazıyor ve `.skip-link:focus-visible` `.skip-link`'i ezdiği için `px-4 py-2` kayboluyordu; focus kuralı `@apply`'dan sonra `padding: 0.5rem 1rem` yazıyor. `<main>` odaklanamadığı için Safari yalnızca kaydırıyordu; `tabIndex={-1}` eklendi, landmark'ta halka bilerek kapalı (`focus-visible:outline-none`, `main` Tab ile ulaşılmaz).
- **`5e9f896` tema anahtarı etiketi.** `aria-label="Toggle theme"` iki mount durumunda da sabit İngilizceydi. `a11y.toggleTheme` anahtarı iki kataloğa eklendi, `nav.test.ts` `a11y` paritesini de karşılaştırıyor. Faz 2 uyarısı "Faz 3 metin yazmaz" içerik metinleri içindi; bu, Task 5'in eklediği `nav.openMenu` gibi bir kontrol etiketi.
- **`57bf733` gölge token'ı.** Planın `color-mix(in oklab, var(--foreground) 35%, transparent)` dizgisi `.dark`'ta `--foreground` beyaza yakın olduğu için gölge yerine hale çiziyordu (Task 4 incelemesi). `--shadow-color` `:root`'ta `oklch(0.2066 0.0101 248.3)` (foreground ile aynı, light çıktısı değişmedi), `.dark`'ta `oklch(0 0 0)`; `.surface-panel` ve `Card` bundan karışıyor.
- **`a9a8705` Tailwind 4.3.3 ve tarama kapsamı.** Faz 0 devir notunun bu faza bıraktığı güncelleme. Lockfile'da yalnızca Tailwind ailesi (dört paket + platform binary'leri) ve geçişli bağımlılıkları (`lightningcss` 1.32.0, `jiti` 2.7.0, `enhanced-resolve` 5.24.5, `tapable` 2.3.3) değişti. Yeni build'in CSS'i eski build ile satır satır karşılaştırılınca planın 4.2'de de var olan bir sorunu ortaya çıktı: Tailwind 4'ün otomatik kaynak tespiti proje kökündeki gitignore edilmemiş her dosyayı tarıyor, `docs/plans/*.md` ve `tests/*.test.ts` içinde alıntılanan sınıf adları (emerald rampası, `rgba(28,22,15,...)` gölgeler, `radial-gradient`, `hover:-translate-y-1`, `focus-visible:ring-[3px]`) derlenmiş stylesheet'e giriyordu; bileşenlerden silinen her şey, silindiğini anlatan belgeler yüzünden CSS'te duruyordu. 4.3 birkaç aday daha çıkardığı için (faz-3 planındaki eski emerald `surface-panel` gölgesi, faz-4 planındaki `px-1.5` / `py-0.5`) fark edildi. `@import "tailwindcss" source("../")` taramayı `src/` ile sınırlıyor; `tests/design-tokens.test.ts` bunu kilitliyor. Kaldırılan sınıflar tek tek `src/` altında arandı, hiçbiri kullanılmıyor.

## Açık kalanlar

- **Dal push edilmedi, PR açılmadı, CI hiç koşmadı.** Dört faz yığılmış durumda; merge sırası ve rebase notu `faz-3-manual-checklist.md` bölüm 1'de. PR gövdesindeki iki yer tutucu (hedef boyutu tablosu, 14 ekran görüntüsü) doldurulmadan PR açılmamalı.
- **14 maddelik görsel kontrol listesi koşulmadı** (tarayıcı yok). Statik olarak doğrulanabilen her şey doğrulandı; gerçekten tarayıcı isteyenler (dialog + `Escape`, odak halkasının görünümü, TR gliflerinin ekranda çizimi, dark mod gölgesi, reduced-motion yeniden boyama, hover durumları) checklist bölüm 3'te. `/contact` maddesi yazıldığı haliyle karşılanamaz: e-posta alanı `required` + `type="email"` olduğu için boş gönderim tarayıcının yerel doğrulamasına takılır, `handleSubmit` hiç çalışmaz, `role="alert"` paragrafı çıkmaz; checklist'te yeniden yazıldı (geçerli alanlar + API hatası).
- **Gizli varyant SSR'da HTML'e yazılıyor.** `useReducedMotion()` sunucuda `null` döndüğü için `reduced` her zaman `false` ve `initial="hidden"` prerender edilmiş HTML'e `style="opacity:0;transform:translateY(4px)"` olarak iniyor (`/` sayfasında 12 eleman, `/projects`'te her satır). Sonuç: reduced-motion kullanıcısı içeriği ilk boyamada görmüyor, hidrasyondan sonra opaklık 1'e sıçrıyor; JS kapalıysa hiç görünmüyor. Plan Step 15 ve checklist'in "içerik ilk boyamada tam opaklıkta" beklentisi bu haliyle karşılanmıyor. Task 6'nın getirdiği bir gerileme değil (Faz 2'nin hero'su da `initial="hidden"` kullanıyordu), planın kendi kodu. Çözüm adayı: gizli durumu CSS sınıfına taşımak (`@media (prefers-reduced-motion: reduce)`'ın etkisiz kıldığı bir keyframe / `data-motion` sınıfı) ya da `initial={false}` ile yalnızca `whileInView` olmadan `animate` kullanmak; ikisi de motion katmanına dokunduğu için sahibinin kararıyla Faz 4 veya ayrı bir düzeltme.
- **Şablon içerik hâlâ yayında ve artık yeni varlıklarla çelişiyor.** `brand` ve `hero.name` "Alex Chen" (header'da DCY işaretinin yanında), `contact.email` `alex@example.com`, `src/data/projects.ts` iki `liveUrl: "https://example.com"`, hero metrikleri (`5+`, `12`, `UI + FE`), `footer.twitter` anahtarı hiçbir yerde kullanılmıyor ama client bundle'a iniyor, `metadata.ogAlt` "Portfolio" / "Portfolyo" (OG kartı artık isim ve unvan taşıyor, alt metin onu anlatmıyor), header'daki eyebrow `Portfolio` JSX'te sabit İngilizce (`header.tsx`, `/tr`'de de "Portfolio"). Planın içerik politikası (satır 26) hepsini Faz 4'e bırakıyor; Faz 4 launch kapısı grep'i `messages/` ve `src/components/layout/header.tsx`'i de kapsamalı.
- **Ölü tanımlar**: `--destructive-foreground` (iki blokta var, `@theme inline`'da yok), `.pull-quote` (hiçbir bileşen kullanmıyor), `footer.twitter`. Faz 4 tüketmezse silinmeli ya da bağlanmalı.
- **Tasarım kararları sahibine**: header marka adının `font-display`'i (Task 5 ile düştü), header `<nav aria-label>`'ının dialog başlığıyla aynı "Menu" olması ("Main" / "Primary" daha ayırt edici), `Dialog.Close asChild` + `Link` yüzünden mobil menü linklerinin `<a type="button">` olarak çıkması (geçerli ama anlamsız öznitelik), OG JSX'inde sabit "Türkiye", 32px favicon'da 10px yüksekliğinde üç harf (okunabilirlik zayıf, tek harf düşünülebilir), `--primary`'nin hero'daki statik eyebrow satırında ve contact'taki `Clock3` ikonunda kullanılması (checklist "yalnızca buton, link hover, available noktası" diyor).
- **Erişilebilirlik ince ayarları** (planın seçimi, inceleme notu): `role="status"` bölgesi metniyle birlikte DOM'a giriyor (kalıcı boş kap daha güvenilir okunur), `aria-busy` `disabled` butonda (etkileşim yolundan çıktığı için anons edilmeyebilir; `aria-disabled` + canlı bölge alternatifi), `:focus-visible { border-radius: inherit }` odaklanan elemanın kendi kutusuna uygulanıyor (ör. `.surface-panel` içindeki düz link odakta 1.5rem köşe alıyor), `body::before` `mask-image` prefix'siz (Safari 15.4 öncesi grid'i sönmeden çizer), masaüstü nav linkleri 36px (`min-h-9`, SC 2.5.8'in 24px eşiğini geçiyor, 44px kuralının tek istisnası).
- **Proxy matcher `icon` prefix eşleşmesi** (`fc3e1f0`): `/((?!api|_next|_vercel|icon|.*\\..*).*)` ileride `/icons` gibi bir yolu da locale rewrite'ından çıkarır (mevcut `api` deseniyle aynı davranış). Yeni kök route'lar gelince `icon(?:$|\\?)` gibi daraltılmalı.
- **`npm run format` CI'da koşmuyor.** Bu fazda iki task (3, 9) bu kapıya takıldı, Faz 2'de dört. `ci.yml` `checks` job'ına bir satır ve `tests/deploy/ci-workflow.test.ts` listesine aynı komut; karar sahibinin (Faz 2 checklist bölüm 6).
- **Testler davranış değil kaynak metni.** Yedi yeni dosyanın hepsi `toContain` / regex; `tests/design-tokens.test.ts`'in `collectTsxFiles` taraması `globals.css`'i kapsamıyor (oradaki `rgba(` koruması yalnızca `rgba(4,120,87` dizgisi), `tests/motion.test.ts` `MotionProvider`'ın layout'a bağlı olduğunu doğrulamıyor, `accessibility.test.ts`'in "44px" testi dosyada `tap-target` geçmesine bakıyor. Bir DOM ortamı (vitest `jsdom` veya Playwright) kurulmadan bunlar yalnızca yanlışlıkla silmeye karşı koruma.
- **24x24 denetim tablosu yalnızca planda** (satır 2609-2622) ve PR gövdesine kopyalanacak; CI'da hiçbir şey doğrulamıyor.
- **Docker deps katmanı `@fontsource*` paketlerini de kuruyor** (`npm ci`, ~684 KB). Runner imajını etkilemiyor (`public`, `.next/standalone`, `.next/static` kopyalanıyor), ağdan font çekilmiyor; `npm ci --omit=dev` builder'ı kıracağı için dokunulmadı.
- **Faz 2'den devralınıp hâlâ açık olanlar**: `[lang]/not-found.tsx` pratikte devreye girmiyor ve 404 dokümanı hep `<html lang="en">` (bu faz 404'e fontları ve token'ları verdi, locale kararı vermedi), sitemap `x-default`, tek `lastModified`, 429/413/okunamayan gövde 400'ü İngilizce, next-intl TypeScript augmentation, blog route'ları, `localesForProject` boş dalı, `locale-url.test.ts` env stub temizliği, README'deki iki deploy bölümü, tüm panel adımları (Coolify, Cloudflare, Traefik, Resend, DNS), `TRUST_CF_CONNECTING_IP`, `.com -> .sh` onayı, Renovate, `npm audit`, CSP nonce (Faz 5). Faz 0'ın bu faza bıraktığı iki madde (`resolvedTheme`, Tailwind 4.3) kapandı.

## Üretilen arayüzler (Faz 4 ve Faz 5 bunları tüketir)

| Arayüz | İmza / şekil |
| --- | --- |
| `@/fonts` (`src/fonts/index.ts`) | `geistSans`, `geistSansExt`, `geistMono`, `geistMonoExt`, `instrumentSerif`, `instrumentSerifExt` (`next/font/local`, her biri kendi `unicode-range`'i ve CSS değişkeni ile; `unicode-range` dizgileri literal olmak zorunda), `fontVariables: string` (`<html className>`'e; layout ve `global-not-found.tsx` ikisi de taşıyor) |
| CSS font değişkenleri | `--font-sans-latin`, `--font-sans-ext`, `--font-mono-latin`, `--font-mono-ext`, `--font-display-latin`, `--font-display-ext` (next/font üretir), `--font-sans-stack`, `--font-mono-stack`, `--font-display-stack` (`:root`); Tailwind `font-sans`, `font-mono`, `font-display` bunlara bağlı; `h1` display, `h2-h4` sans |
| Renk token'ları | `--status-up`, `--status-down` (Tailwind: `bg-status-up`, `text-status-up`, `bg-status-down`, `text-status-down`), `--shadow-color` (tema başına; `color-mix` ile gölge), `--primary` / `--ring` emerald'ın kaldığı tek yerler; `--destructive-foreground` tanımlı ama `@theme`'de yok |
| Utility sınıfları (`globals.css`) | `.tap-target` (44x44), `.skip-link` (+ `:focus-visible` görünür), `.pull-quote` (display serif), `.eyebrow` (mono, uppercase), `.surface-panel` (nötr gölge, `bg-card/80`, blur), `.page-shell*`, `.section-space`, `.section-title`, `.section-copy` |
| Tailwind kaynak kapsamı | `@import "tailwindcss" source("../")`: yalnızca `src/` taranır. `src/` dışında sınıf adı taşıyan bir UI dosyası eklenirse `@source` ile bildirilmeli; `docs/` ve `tests/` içindeki sınıf alıntıları artık CSS'e girmiyor |
| `@/lib/nav` | `navItems: readonly { href: "/" \| "/about" \| "/projects" \| "/contact"; key: "nav.*" }[]` (`as const`), `type NavItem`; header, mobil menü ve footer aynı listeyi okur |
| `@/lib/motion` | `STAGGER_SECONDS = 0.04`, `MAX_STAGGER_ITEMS = 4`, `staggerDelay(index): number` (4'te kilitlenir), `fadeUp(reduced): Variants`, `staggerContainer(reduced): Variants`, `staggerItem(reduced): Variants`; `reduced` `useReducedMotion() ?? false` ile verilir |
| `@/components/motion-provider` | `MotionProvider({ children })`: `LazyMotion features={domAnimation} strict`; layout'ta `NextIntlClientProvider` içinde. Bileşenler `m`'i `motion/react-m`'den alır; eager `motion` importu strict altında çalışma zamanında hata verir, `tests/motion.test.ts` `src/**/*.tsx`'in tamamını tarar |
| `@/components/layout/mobile-menu` | `MobileMenu()`: Radix `Dialog`, `md:hidden` tetikleyici, `navItems` listesi, `aria-describedby={undefined}`; `nav.menu`, `nav.openMenu`, `nav.closeMenu` |
| `@/components/sections/project-row` | `ProjectRow({ project: Project; index: number; variants?: Variants })`: `m.li`, yıl mono, başlık link (`@/i18n/navigation` `Link`), rol, ilk 4 tag; blog satırı için türetilecek desen |
| `@/components/sections/project-card` | Kart artık `Link` ile sarılmıyor; başlık link, `after:absolute after:inset-0` overlay tıklama alanı; kapak alanı bilinçli boş |
| `@/components/layout/theme-toggle` | `resolvedTheme` okur, `a11y.toggleTheme` etiketi; `useSyncExternalStore` ile mount koruması |
| `<main id="main" tabIndex={-1}>` + `.skip-link` | Skip link hedefi; yeni kök doküman yazılırsa aynı çift oraya da gerekli |
| Mesaj anahtarları | 158 anahtar, 13 namespace (`a11y` yeni): `nav.menu`, `nav.openMenu`, `nav.closeMenu`, `footer.navTitle`, `a11y.skipToContent`, `a11y.toggleTheme`; `tests/nav.test.ts` üst düzey, `nav`, `footer`, `a11y` paritesini kilitler |
| `src/app/[lang]/opengraph-image.tsx`, `src/app/icon.tsx` | `siteConfig.person` kaynaklı; OG fontları `public/fonts/og/instrument-serif-latin.woff` (`Instrument Serif`) ve `-latin-ext.woff` (`Instrument Serif Ext`, ayrı aile adı; satori `unicode-range` bilmez); `favicon.ico` yok, tek ikon kaynağı `/icon` |
| `src/proxy.ts` | matcher `/((?!api|_next|_vercel|icon|.*\\..*).*)`; kök seviyede nokta içermeyen yeni bir route (ör. `/manifest`) eklenirse lookahead'e girmeli |
| `scripts/vendor-fonts.mjs` | `npm run vendor:fonts`: `@fontsource*` paketlerinden latin / latin-ext woff2 ve OG woff'larını kopyalar, deterministik; paket sürümü değişince yeniden koşulup diff commit'lenir |
| Test dosyaları | `tests/fonts.test.ts`, `tests/design-tokens.test.ts`, `tests/motion.test.ts`, `tests/nav.test.ts`, `tests/accessibility.test.ts`, `tests/og-image.test.ts`, `tests/plans/faz-3-plan.test.ts` |

## Sonraki faza (Faz 4, içerik ve yayın) uyarılar

- **İki kök doküman var.** `src/app/[lang]/layout.tsx` ve `src/app/global-not-found.tsx` ikisi de `fontVariables`'ı `<html>`'e, `ThemeProvider`'ı body'ye koyuyor; layout'a eklenen hiçbir şey 404'e otomatik gitmez. `tests/i18n/app-shell.test.ts` font paritesini kilitliyor; header/footer/skip link 404'te yok, locale'e özel 404 kararı Faz 2'den beri açık.
- **Yeni bileşen reçetesi**: `"use client"` + `m` (`motion/react-m`) + `useReducedMotion() ?? false` + `@/lib/motion` fabrikaları; `motion/react`'ten yalnızca `useReducedMotion` ve tipler. Liste animasyonunda `custom={index}` ile `fadeUp` ya da `staggerContainer` + `staggerItem`; gecikme 4 elemanda kilitlenir. `whileInView` yasak (test). Gizli varyantın SSR'a yazılması "Açık kalanlar"daki karar verilene kadar bilinçli sınır.
- **Renk disiplini**: `emerald-*`, `rgba(`, `radial-gradient`, hex renk `src/**/*.ts(x)` içinde yasak (`tests/design-tokens.test.ts` tarıyor, `globals.css` taramada değil). Vurgu için `text-primary` / `bg-primary`, durum için `*-status-up` / `*-status-down`, gölge için `--shadow-color`; `bg-accent/*` ve `hover:-translate-y-*` kullanılmaz (header ve project-card'daki kalıntılar Task 5-7'de temizlendi).
- **Blog satırı** `project-row.tsx`'ten türetilir: `ul > m.li`, mono tarih, başlık `Link`, sağda meta; kapak yok. `projects-section.tsx` filtre butonları `min-h-9`, satırlar `py-5`; aynı ölçüler blog'a taşınabilir.
- **Nav'a route eklemek**: `src/lib/nav.ts` + iki katalogda `nav.<key>` + `scripts/assert-static-routes.mjs` `required` listesi + `tests/nav.test.ts` beklentileri; header masaüstü listesi, mobil dialog ve footer "Pages" bloğu otomatik gelir.
- **Kontrol etiketleri `a11y` namespace'inde**, içerik metinleri kendi namespace'lerinde. Faz 4 içerik geçişinde anahtar yapısı değişmez; `footer.twitter` silinebilir, `metadata.ogAlt` kartı anlatacak şekilde yeniden yazılmalı (ör. "Doğan Can Yıldız, Full-Stack Web Developer and DevOps Engineer"), `header.tsx`'teki sabit "Portfolio" eyebrow'u bir anahtara bağlanmalı.
- **Fontlar**: yeni bir yüz gerekirse `scripts/vendor-fonts.mjs` `FONTS` listesine girer, `src/fonts/index.ts`'e literal `unicode-range` ile yükleyici eklenir, `fontVariables`'a değişkeni girer. `preload: true` yalnızca üç latin yüzde; mono'nun preload'u ilk ekranda kullanılmıyorsa düşürülebilir (Task 1 incelemesi). `@fontsource*` paketleri yalnızca vendor script'i için devDependency; `next/font/google` yasak.
- **OG görselini değiştirirken** `generateImageMetadata`'nın boş params ile ilk çağrısı (`hasLocale` fallback'i), `OG_IMAGE_*` sabitleri (`src/lib/seo/og-image.ts`) ve iki woff'un ayrı aile adları korunmalı; `tests/og-image.test.ts` ve `tests/seo/page-metadata.test.ts` kilitliyor. Yeni bir kök metadata route'u (`manifest`, `apple-icon`) eklenirse `src/proxy.ts` matcher'ına da girmeli, yoksa `/icon` gibi 404 döner.
- **Tailwind 4.3.3 ve `source("../")`**: `src/` dışına UI dosyası koymayın; `docs/` ve `tests/` içinde sınıf alıntılamak artık güvenli. `npm update` ile gelen bir sonraki Tailwind sürümünde derlenmiş CSS'i eski build ile satır bazında karşılaştırmak (bu turda yapılan yöntem) ucuz ve öğretici.
- **Prettier kapsamı ve kapı sırası**: `tests/`, `scripts/`, `src/`, `messages/` Prettier'a tabi, `docs/` değil; commit'ten önce `npm run typecheck && npm run lint && npm run format && npm test`, build'den sonra `npm run verify:routes`. Bu fazın on inceleme düzeltmesinin ikisi Prettier'dı.
- **Docker**: `public/fonts/og` runner'a `public` ile, `src/fonts` `.next/static/media` ile gidiyor; Dockerfile'a dokunmak gerekmedi ve gerekmiyor. `.dockerignore`'a `*.woff*` gibi bir desen eklenmemeli.
- **CI check adları değişmedi** (`lint, typecheck, test, build`, `hadolint and image build`); `checks` job'ı altı `run` adımı.

## Manuel adımlar

Kodla yapılamayan her şey `docs/plans/handoffs/faz-3-manual-checklist.md` içinde, sıra bağlayıcı: (1) dalı push edip PR açmak (dört fazlık yığın sırası, PR gövdesindeki iki yer tutucunun doldurulması, diff hijyeni), (2) CI'da altı adım + Docker job'ının geçtiğini görmek, (3) tarayıcıda 14 maddelik görsel liste artı bu turun eklediği maddeler (dark mod gölgesi, skip link, TR tema etiketi, 404 sayfası, dialog odak tuzağı), (4) reduced-motion SSR kararı, (5) Coolify preview ve canlıda font / OG / favicon doğrulaması, (6) sahibinin kararını bekleyen tasarım ve içerik maddeleri, (7) Faz 0/1/2'den devralınan açık maddeler.

## Task başına model ve inceleme özeti

| Task | Model | Commit | Bloklayan bulgu | Düzeltme | Bloklamayan bulguların özü |
| --- | --- | --- | --- | --- | --- |
| 0 dal ve devralınan arayüzler | haiku | `21895d0` (plan) | Plan tablosunda OG route'u yanlış (kökte yok, `[lang]` altında); `global-not-found.tsx` planda hiç yok | `21895d0` | Dal `main`'den açılamadı (PR'lar merge edilmemiş); `createNavigation` grep beklentisi 1 yerine 2; `vitest.config.mts`; `useTranslations("nav")` / `("notFound")` iki scope'lu istisna; `resolvedTheme` ve Tailwind 4.3 Faz 0 devirleri; `format`, `build`, `verify:routes` başlangıçta yeşil |
| 1 font vendoring | sonnet | `32e937c` | yok | yok | Testler `unicode.json` ile aralık eşitliğini doğrulamıyor (elle doğrulandı, glifler fontTools ile); üç preload ~72 KB (mono tartışılabilir); commit gövdesindeki "Docker never needs the font packages" geniş ifade (`npm ci` devDeps'i kuruyor); build koşulmadı (Task 2'de koşuldu) |
| 2 fontları bağla | sonnet | `aef6e7c` | Yüzler hiçbir elemana uygulanmıyor (`fontVariables` body'de, stack `:root`'ta) | `153ccc9` | `design-tokens.test.ts` tamamen kaynak grep'i; `app-shell.test.ts`'te describe iç içe yanlış kapanmış; CSS yolu `.next/static/chunks`; plan dışı iki dosya (`src/fonts/index.ts` inline, `page-metadata.test.ts` mock); yorumdaki hata metni yanlış; `.pull-quote` kullanılmıyor; regex Prettier satır kırılımını sabitliyor |
| 3 nötr palet | haiku | `08c8306` | Altıncı test (solid ring) yazılmamış; iki dosyada Prettier kırık | `0343c45`, `62285f1` | `.surface-panel` gölgesi Task 4 yerine burada silindi; commit mesajı plandan farklı; `--destructive-foreground` ölü; "emerald hex yok" testi trivially geçiyor; palet tablosu ve kontrastlar birebir doğrulandı; plan checkbox'ları işaretlenmemiş |
| 4 zemin ve doğrudan renkler | sonnet | `4fba5c5` | yok | yok (entegrasyon `57bf733`) | Dark modda gölge ters (planın dizgisi); hijyen taraması CSS'i kapsamıyor; `language-switcher` satır referansları eskiydi; header'da "DCY" + "Alex Chen" yan yana; `destructive`'de de translate kaldırıldı; `mask-image` prefix'siz |
| 5 mobil menü ve footer | sonnet | `92fbe6f` | yok | yok | Plan koduna birebir; `a11y.skipToContent` o an tüketilmiyordu (Task 8 bağladı); `footer.twitter` ölü; şablon persona; `<a type="button">`; marka `font-display`'i kaybetti; header `aria-label` = dialog başlığı; `nav.test.ts` plan dışı ve kaynak grep'i; tarayıcı yarısı koşulmadı; masaüstü nav 36px |
| 6 LazyMotion + m | sonnet | `60c9605` | yok | yok | reduced-motion SSR'da etkisiz (12 gizli stil `/`'da), plan Step 15 beklentisi karşılanmıyor; `theme-toggle` `resolvedTheme` (Task 10'da kapandı); plan listesi dışı üç dosya taşındı, Step 12 zaten yapılmıştı; commit mesajı plandan farklı; `MotionProvider` bağlantısı testsiz, import regex'i dar; checkbox'lar |
| 7 satır formatı | sonnet | `04d5bd2` | yok | yok | Plan "46 test" eski; plan test istemiyordu, üç iddia eklendi; kod plana birebir |
| 8 erişilebilirlik | sonnet | `5295f98` | `outline-none` yeni halkayı `button`/`input`/`textarea`'da siliyor | `c355a50` (entegrasyon `9ff17df`, `5e9f896`) | Skip link odakta padding kaybediyor; `<main>` odaklanamıyor; ölü `ring-destructive/*` sınıfları; `border-radius: inherit` yan etkisi; `role="status"` ve `aria-busy` deseni; testler kaynak grep'i; `theme-toggle` İngilizce etiket ve `theme`; eşzamanlı ajan dosyaları geri aldı; brief'teki `709d860` amend öncesi hash |
| 9 OG ve favicon | haiku | `933fa71` | TR latin-ext harfleri OG'de Geist'e düşüyor; Prettier üç dosyada kırık; `GET /icon` 404 | `377ec06`, `fc3e1f0` | `ogAlt` şablon ve commit gövdesi yanlış iddia; `"Portfolio</p>"` ölü assertion; testler görsel üretmiyor; ülke adı sabit; favicon monogramı küçük; iki OG görseli 200 ve md5 kararlı |
| 10 kapanış | haiku | yok | `favicon.ico` eski üçgen ve `/icon`'un önünde; `theme-toggle` `theme` okuyor | `1d5be08`, `598608e` | Tailwind 4.2 (entegrasyon `a9a8705`); ekran görüntüleri koşulamadı; devir notu yoktu; PR açılmadı; gizli varyant SSR'da; `/contact` maddesi karşılanamaz; proxy `icon` prefix'i; şablon içerik; testler kaynak grep'i; accent kullanımı iki statik yerde daha; kapıların tamamı yeşil |
| entegrasyon | fable | `9ff17df`, `5e9f896`, `57bf733`, `a9a8705`, bu not | kapılar ve on kriter yeşil | yok | Tailwind kaynak tarama bulgusu (61.9 KB -> 47.6 KB); bu notun "Açık kalanlar" bölümü |
