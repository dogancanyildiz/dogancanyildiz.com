# i18n Mimarisi (EN/TR)

Durum: Uygulandı (Faz 2, PR #4; Faz 4'te genişletildi; 2026-08-30 TR varsayılan + yerelleştirilmiş path; 2026-09-02 proxy dışlamaları statik ikon dosyalarına göre sadeleşti) · Karar: 2026-08-27 · Güncelleme: 2026-09-02 · Kapsam: dogancanyildiz.com

## Özet

Mevcut i18n kurulumu tamamen cookie tabanlı: tek URL (dogancanyildiz.com/about) hem EN hem TR içeriği NEXT_LOCALE cookie'sine göre sunuyor, bu yüzden hreflang teknik olarak kurulamıyor ve Googlebot cookie göndermediği için TR içerik pratikte indekslenemiyor. Bu doküman cookie yaklaşımının neden terk edildiğini, URL stratejisi (as-needed / always / subdomain) ve kütüphane (next-intl / Paraglide / next-international / next-i18next / bağımlılıksız elle [lang]) seçimlerinin gerekçesini ve app/[lang] + next-intl 4.13.7 kurulumunun somut adımlarını kaydeder. Karar: next-intl 4.13.7, localePrefix "as-needed" (TR kökte prefix'siz, EN `/en` altında), proxy.ts ile localeDetection kapalı. Bağımlılıksız elle [lang] yolunun da tam statik üretimle çalıştığı doğrulanmış durumda ve next-intl bir Next major'ında takılırsa veya setRequestLocale disiplini sistemli şekilde bozulursa geri dönülecek yol olarak kayıtlı.

## Kararlar

1. **URL stratejisi**: subdirectory + prefix, `localePrefix: "as-needed"`. **Karar değişikliği (2026-08-30):** varsayılan dil Türkçe. TR kökte kalır (`/`, `/hakkimda`, `/projeler`, `/iletisim`). EN `/en` altında yaşar (`/en`, `/en/about`, `/en/contact`). `defaultLocale: "tr"`, `locales: ["en", "tr"]`. Nav slug'ları `pathnames` ile yerelleşir; proje ve yazı slug'ları her iki dilde aynı kalır.
2. **Kütüphane**: next-intl 4.13.7. Bağımlılıksız elle `[lang]` yolu reddedilmedi, next-intl için tanımlı bir tripwire koşuluyla yedek plan olarak tutuluyor (bkz. Riskler ve tripwire'lar).
3. **Routing dosyası**: Next.js 16 konvansiyonuna göre `middleware.ts` değil `proxy.ts` yazılır; `localeDetection: false` set edilir, yani Accept-Language header'ına göre otomatik yönlendirme yapılmaz.
4. **Static rendering**: her page ve layout'ta `setRequestLocale` + `generateStaticParams` çağrılır, iki locale de build zamanında prerender edilir.
5. **Mesaj kaynağı**: `src/lib/i18n/translations.ts`, `src/components/locale-provider.tsx`, `src/lib/i18n/use-translation.ts` ve dört layout dosyasındaki `cookies()` çağrıları kaldırılır; yerine `messages/en.json` ve `messages/tr.json` gelir.
6. **Çeviri politikası**: çevirisi olmayan içerik için fallback sayfa üretilmez. O slug, o dilin `sitemap.ts` girdisine ve `alternates.languages`'ına hiç eklenmez.

## Gerekçe

### Cookie yaklaşımı neden savunulamaz

Mevcut kurulum üç ayrı sorunu tek noktada birleştiriyor:

- **Tek URL, iki dil.** `dogancanyildiz.com/about` hem EN hem TR içeriği aynı adresten sunuyor; hangi dilin döneceği yalnızca `NEXT_LOCALE` cookie'sine bağlı. Googlebot cookie taşımadığı için TR içerik crawl edilebilir bir URL'e hiç sahip değil.
- **hreflang imkansız.** hreflang, her dil için ayrı ve kalıcı bir URL gerektirir (developers.google.com/search/docs/specialty/international/localized-versions). Aynı URL'de iki dil varken `alternates.languages` yazılacak ikinci bir adres yok.
- **generateMetadata cookie okuyor.** `src/app/layout.tsx:11-13` içindeki `generateMetadata`, `cookies()` üzerinden locale'i okuyup title/description'ı buna göre üretiyor. Aynı URL isteğin cookie'sine göre farklı içerik döndürmesi, arama motorunun iki dili ayrı sayfa olarak görmesini engelliyor ve teknik olarak cloaking'e yakın bir davranış sayılıyor.
- **8/11 route dynamic'e düşüyor.** `cookies()` çağrısı App Router'da tüm ağacı dynamic rendering'e zorluyor. Toplam dört dosyada beş ayrı `cookies()` çağrısı var: `src/app/layout.tsx:11` ve `:30` (root layout, hem `generateMetadata` hem `RootLayout` içinde), `src/app/about/layout.tsx:6`, `src/app/contact/layout.tsx:6`, `src/app/projects/layout.tsx:6`. Sentez kaydına göre bu, build çıktısındaki 11 route'tan 8'inin statik yerine dynamic üretilmesine yol açıyor; static prerender, ISR ve edge cache avantajlarının hiçbiri kullanılamıyor.

### URL stratejisi: neden as-needed

| Seçenek | Artılar | Eksiler | Uyum skoru (10 üzerinden) |
|---|---|---|---|
| **as-needed** (varsayılan dil prefix'siz kökte, diğeri `/xx` altında; tarihsel: 2026-08-27 karar anında EN kökteydi, 2026-08-30'da TR köke taşındı, bkz. Karar 1) - seçilen | next-intl'in resmi örnek konfigürasyonu bu mod; kök gerçek içerik sunar, redirect atlaması yok; iki ayrı crawl edilebilir URL kümesi Google'ın her dili bağımsız indekslemesini sağlar; hreflang + x-default kurulumu net (x-default ve varsayılan dilin kök URL'ine, diğeri `/xx/...`'a işaret eder) | Asimetrik yapı (kökteki dil "ana", diğeri "ikincil" görünür), bilinçli bir karar olmalı; varsayılan middleware kökte Accept-Language'e göre otomatik yönlendirme yapabilir (bilinçli kapatılmalı); üçüncü bir dil eklenirse asimetri karmaşıklaşabilir | 8 |
| **always** (`/en`, `/tr`) | Simetrik yapı, "hangi dil ana dil" belirsizliği yok; hreflang eşleştirmesi en basit hali; kök ayrı bir dil seçim sayfası olabilir | Mevcut URL'ler `/xx/...`'a taşınır, 301 zinciri gerekir; kullanıcının o zamanki tercihi (tarihsel: EN prefix'siz, 2026-08-30'dan beri TR prefix'siz) ile uyuşmuyor; portföy ölçeğinde simetri getirisi pratik dezavantajlardan küçük | 6 |
| **subdomain** (`tr.dogancanyildiz.com`) | Farklı altyapı/platform gerekiyorsa izolasyon sağlar (bu projede geçerli değil) | Google alt domainleri pratikte ayrı site gibi ele alıyor, link equity konsolidasyonu subdirectory kadar güvenilir değil; Coolify/Traefik'te ekstra DNS + sertifika + routing kaydı gerektirir; tema/tercih state'i domain sınırları arasında paylaşılmaz | 2 |

**Not (düzeltilen gerekçe):** İlk i18n araştırması as-needed seçimini kısmen "mevcut EN URL'lerin (dogancanyildiz.com/about) korunması" ile gerekçelendirmişti. Bu gerekçe geçersiz: dogancanyildiz.com yeni bir ana domain, korunacak eski veya indekslenmiş bir URL kümesi yok. **Not (2026-08-30):** bu gerekçe TR-varsayılan geçişinde kısmen ters döndü: EN URL'ler kısa süre kökte yayında kaldığı için `src/i18n/legacy-en-paths.ts` eski öneksiz İngilizce nav yollarını kalıcı 308 ile `/en/...`'e taşıyor. Gerçek gerekçe, kökün gerçek içerik sunması (redirect atlaması olmadan), x-default'un köke işaret etmesi ve paylaşılan linklerin kısa kalmasıdır. Karar (as-needed) değişmedi, yalnızca gerekçesi düzeltildi.

### Kütüphane seçimi: neden next-intl

| Kütüphane | Güncel sürüm | Son yayın | Uyum skoru | Not |
|---|---|---|---|---|
| **next-intl** - seçilen | 4.13.7 | 2026-08-21 | 9 | App Router + Server Components için baştan tasarlanmış; Next.js 16 root-params ve proxy.ts ile güncel resmi örnekler var (next-intl.dev); ICU çoğul/tarih/sayı formatlaması hazır; TypeScript ile mesaj anahtarları tip güvenli |
| Paraglide JS (adapter'sız) | 2.24.1 | 2026-08-15 | 7 | Compiler tabanlı, kullanılmayan mesajlar tree-shake edilir (opral/paraglide-js: "up to 70% smaller"); Next.js 16 App Router'ı 2.0'dan beri adapter'sız destekliyor; ama Next'e özgü rotalama/hreflang/sitemap dokümantasyonu next-intl kadar olgun değil, eski `@inlang/paraglide-next` adapter'ı 2025-03'ten beri güncellenmemiş |
| next-international | 1.3.1 | 2024-10-31 | 3 | npm `time.modified` alanına göre ~22 aydır güncellenmemiş; Next.js 15/16, proxy.ts, root-params değişikliklerine uyup uymadığı belirsiz; uzun ömürlü bir kişisel site için bakım riski |
| next-i18next (App Router) | 16.0.11 | 2026-08-23 | 4 | App Router desteği 2026 Mart'ta geldi, henüz olgunlaşmadı; i18next + react-i18next + next-i18next üç katmanı birlikte ~1.6MB unpacked, next-intl'den belirgin şekilde daha ağır; bu proje sıfırdan App Router üzerine kuruluyor, Pages Router mirası taşıyan bir kütüphaneyi tercih etmenin somut getirisi yok |
| Bağımlılıksız elle `[lang]` + mevcut sözlük | - | - | doğrulandı, skorlanmadı | maintenance-cost doğrulayıcısı ~40 satır değişiklikle ve sıfır yeni bağımlılıkla tam statik üretimin çalıştığını build ederek gösterdi; gerçek bir seçenek ama ICU çoğul/tarih formatlamayı ve her `Link`'te locale prefix'ini elle taşımayı blog büyüdükçe sürekli bir doğruluk yüküne çeviriyor |

next-intl'in tercih edilmesinin üç somut gerekçesi var: (1) ICU mesaj sözdizimi, TR'de gerekli çoğul/sayı/tarih formatlamayı Intl API sarmalayıcısıyla hazır veriyor, mevcut elle yazılmış `translate()`/`getNested()` (`src/lib/i18n/translations.ts`) bunu desteklemiyor; (2) aktif bakım, en son sürüm bu dokümandan önceki hafta yayınlanmış; (3) Next.js 16'nın root-params ve proxy.ts değişikliklerine güncel, resmi örnekler bakımcı tarafından doğrulanmış durumda. Bağımlılıksız yolun kanıtlanmış olması bu kararı zayıflatmıyor, tam tersine next-intl bir noktada tıkanırsa elde hazır bir B planı olduğu anlamına geliyor (bkz. tripwire).

## Reddedilen Alternatifler

- **always (`/en`, `/tr`) URL şeması**: teknik olarak daha simetrik ama bu projede taşınacak eski URL yok, simetri getirisi asimetrik yapının pratik dezavantajlarından küçük.
- **subdomain (`tr.dogancanyildiz.com`)**: tek uygulama, tek sunucu, iki dil için gerekçesi yok; Google ve pratik kaynaklar yeni projelerde subdirectory'yi öneriyor.
- **next-international 1.3.1**: son yayın 2024-10-31, aktif geliştirme sinyali zayıf; yeni bir projede başlangıç noktası olarak önerilmez.
- **next-i18next**: i18next ekosistemine bağımlılık/tercih yoksa bu proje için gerekçesi zayıf; App Router desteği çok yeni.
- **Paraglide JS**: bundle boyutu öncelikliyse (framer-motion/motion + radix-ui zaten ağır) güçlü bir alternatif, ama Next.js'e özgü rotalama/hreflang/sitemap olgunluğu next-intl'in gerisinde; adapter paketi güncellenmiyor.
- **Astro'nun yerleşik i18n'i**: yalnızca karşılaştırma amacıyla değerlendirildi, bu Next.js kod tabanı için önerilmiyor. Astro'nun i18n'i kendi dokümanının deyimiyle sadece bir routing API'si; mesaj sözlüğü ve ICU çoğul/tarih formatlaması yok. Framework değişimi ayrı bir mimari karar, i18n bu kararı tetiklememeli (bkz. 02-stack-karari.md).
- **Bağımlılıksız elle `[lang]`**: reddedilmedi, ertelendi. next-intl'in kütüphane katmanı (ICU, tipli mesajlar, locale-doğru Link/redirect helper'ları) blog büyüdükçe elle taşınacak yükten daha ucuz görüldü. Aşağıdaki tripwire koşullarından biri gerçekleşirse bu yola dönülür.

## Uygulama durumu (2026-08-27)

Routing, proxy, mesaj kaynağı, hreflang ve sitemap Faz 2'de (dal `feature/faz-2-i18n-app-lang`, PR #4, merge commit `d65991d`) uygulandı; içerik modeli, blog dil politikası ve birkaç küçük düzeltme Faz 4'te (dal `feature/faz-4-icerik-ve-yayin`, PR #6, henüz merge edilmedi, HEAD `8b4fe40`) genişletildi.

**Routing/proxy.** `src/i18n/routing.ts` kararla birebir: `locales: ["en", "tr"]`, `defaultLocale: "tr"` (2026-08-30; tarihsel değer "en" idi), `localePrefix: "as-needed"`. `src/proxy.ts` `createMiddleware(routing)` çağırıyor. `config.matcher` bugün `["/:path*"]`, yani her yol: `global-not-found` 404 dilini `x-pathname`'den okuyor ve proxy'nin atladığı bir yol, istemcinin o başlığı kendi gönderebildiği bir yol demekti. i18n dışlamaları `src/lib/locale-from-pathname.ts`'teki `isLocalizedRoutePath`'e taşındı: `/api`, `/_next`, `/_vercel` önekleri ve son segmentinde nokta taşıyan her yol (dosyalar) dışarıda, locale önekli yollar ve `/feed.xml` içeride. 2026-09-02'de uzantısız istisna listesi (`/icon`, `/apple-icon`) kalktı; ikonlar artık `favicon.ico`, `icon.png`, `apple-icon.png` statik dosyaları, yani zaten nokta kuralına giriyorlar. `localeDetection` kararla uyumlu, `createMiddleware(routing)` içinde routing objesinden geliyor (Accept-Language yönlendirmesi kapalı).

**Mesajlar.** Cookie tabanlı eski `src/lib/i18n/translations.ts`, `locale-provider.tsx` ve `use-translation.ts` kaldırıldı; `messages/en.json` ve `messages/tr.json` kullanılıyor, `tests/messages.test.ts` iki kataloğun 97 anahtar / 13 namespace'te parite ve tüketim eşleşmesini kilitliyor (Faz 4, `5e36d7d`).

**Static rendering.** `setRequestLocale` + `generateStaticParams` her sayfa ve layout'ta çağrılıyor; doğrulama artık elle değil `scripts/assert-static-routes.mjs` (`npm run verify:routes`) ile CI'da otomatik (Faz 4). Faz 4 HEAD'inde 26 içerik route'u statik (5 proje x 2 locale, 1 EN yazı, 3 TR yazı); yalnızca `/api/contact` ve `/api/health` dynamic kalıyor.

**Fallback-yok politikası.** Karar 6 Velite tabanlı içerik katmanıyla uygulandı: `content/blog/en/` yalnızca `self-hosting-with-coolify.mdx` içeriyor, `content/blog/tr/` üç yazı içeriyor (`self-hosting-with-coolify.mdx`, `capt-sinavina-hazirlik.mdx`, `ccna-dan-web-guvenligine.mdx`); çevirisi olmayan slug diğer dilin `generateStaticParams`'ında, `sitemap.ts`'inde ve `alternates.languages`'ında hiç görünmüyor. `src/i18n/switch-target.ts` bu politikayla tutarlı: çevirisi olmayan bir proje/yazı detay sayfasında dil değiştirici `/blog` veya `/projects` bölüm köküne, diğer her yolda `/` köküne düşüyor (404'e değil).

**Blog dil politikası (TR-first).** İçerik sayımı yukarıdaki gibi doğrulandı: 1 EN + 3 TR. Çekirdek sayfalar (Home, About, Projects, Contact) iki dilde de eksiksiz.

**Diğer küçük düzeltmeler (Faz 4).** `src/i18n/request.ts:17` `timeZone: "UTC"` set ediyor. `src/app/api/contact/route.ts` locale'i `[lang]` root param'ından değil gövdeden okuyor (`resolveLocale`, Route Handler kısıtı gereği); bilinmeyen/eksik değer `routing.defaultLocale`'a düşüyor. `/favicon.ico -> /icon` 308 yönlendirmesi kurulmuştu; 2026-09-02'de kaldırıldı, `src/app/favicon.ico` gerçek dosya olarak 200 dönüyor.

**Sapmalar:** `AGENTS.md` ve `CLAUDE.md` `.gitignore`'a eklendi (`next dev` bu dosyaları üretiyor); i18n kararının kendisiyle doğrudan ilgisi yok ama entegrasyon turunda bu dalda yapıldı, sahibi isterse geri alabilir.

**Hâlâ açık olanlar:**
- `src/app/global-not-found.tsx` (rota hiç eşleşmeyen istekler için, `[lang]` dışı) `<html lang>` ve `generateMetadata` başlığını hep `routing.defaultLocale` (en) ile üretiyor; gövdeye ikinci dilde (tr) bir paragraf ve geri dön linki ekliyor ama belge dili sabit en kalıyor. `[lang]/not-found.tsx` (route eşleşip `notFound()` fırlatılan durumlar için) doğru locale'i kullanıyor, bu ayrım sürüyor.
- `src/app/api/contact/route.ts:18,35,61` içindeki 413 mesajı (`TOO_LARGE_MESSAGE`) sabit İngilizce string, `t()` üzerinden gitmiyor; 429 mesajı (`:43-48`) `t("tooManyRequests")` kullanıyor ama locale gövde henüz okunmadığı için her zaman `routing.defaultLocale` (en) ile çözülüyor. İkisi de TR isteklerde İngilizce dönüyor.
- `sitemap.ts` içinde satır bazında `x-default` girdisi yok; `src/lib/seo/alternates.ts:107` `languages["x-default"]`'ı `<head>`'deki `alternates.languages`'a yazıyor, sitemap'e değil. Bu doğru davranış (sitemap'te x-default anlamlı değil) ama doküman bunu netleştirsin diye not edildi.

## Riskler ve Tripwire'lar

- **Acil, kozmetik değil.** Cookie-only mevcut yapı prod'da bırakılırsa TR içerik Google tarafından pratikte indekslenemez. Faz 2 (i18n yeniden mimarisi) launch'tan önce tamamlanmalı.
- **TRIPWIRE - setRequestLocale disiplini.** Static rendering her page/layout'ta `setRequestLocale` çağrısına bağlı; unutulan bir sayfa build hatası vermeden sessizce dynamic rendering'e düşer, yalnızca performans/ISR davranışı bozulur, CI bunu yakalamaz. Doğrulama: `next build` çıktısında route tipleri (○ Static / ƒ Dynamic) her PR'da elle veya bir script ile kontrol edilmeli.
- **TRIPWIRE - next-intl bir Next major'ında takılırsa.** next-intl bir Next.js major sürümünde uzun süre uyumsuz kalırsa veya setRequestLocale disiplini defalarca sessizce bozulursa, bağımlılıksız elle `[lang]` yoluna geçilir (doğrulanmış B planı, bkz. Kütüphane seçimi tablosu).
- **Otomatik Accept-Language yönlendirmesi kapalı tutulmalı.** next-intl'in varsayılan proxy davranışı kökte Accept-Language'e göre otomatik yönlendirme yapabilir; Google bunu açıkça önermiyor (support.google.com/webmasters thread, merj.com). `localeDetection: false` bilinçli seçilmeli, Googlebot'un crawl'ini etkilememeli.
- **hreflang hata oranı yüksek.** Kurulumların yaklaşık %75'inde hata bulunduğu raporlanıyor (dchost.com); tek bir yanlış dil kodu veya eksik self-referencing/return tag tüm hreflang cluster'ının yok sayılmasına yol açabilir. `sitemap.ts` ve `generateMetadata`'daki `alternates.languages` Search Console ve gerçek bir hreflang test aracıyla elle doğrulanmalı (detay: 07-seo-ve-metadata.md).
- **MDX içerik modeli için resmi çözüm yok.** next-intl GitHub discussion #355'e göre blog MDX için resmi bir "content collection" çözümü yok; Velite tabanlı loader kurulmalı ve yalnızca çevirisi olan dilin sitemap/hreflang'a eklenmesi disiplinli şekilde elle yönetilmeli, aksi halde çevrilmemiş fallback sayfalar yanlışlıkla indekslenebilir (miaoosi.com uyarısı). Detay: 05-backend-icerik-ve-servisler.md.
- **Türkçe büyük/küçük harf dönüşümü.** JS'in varsayılan `toUpperCase()`/`toLowerCase()` Türkçe locale kurallarını (İ/ı, dotless-i) bilmiyor. `"İstanbul".toLowerCase()` gibi çağrılar İngilizce kurallarına göre yanlış sonuç üretir; TR metinlerde harf büyütme/küçültme gereken her yerde `toLocaleUpperCase("tr-TR")` / `toLocaleLowerCase("tr-TR")` kullanılmalı (kaynak: Wikipedia, Dotted and dotless I in computing).

## Uygulama Notları

### Dosya yapısı

```
src/app/[lang]/
  layout.tsx          # setRequestLocale çağırır
  page.tsx             # ana sayfa (generateStaticParams: [{lang: "en"}, {lang: "tr"}])
  about/page.tsx
  projects/page.tsx
  projects/[slug]/page.tsx
  contact/page.tsx
src/i18n/
  routing.ts           # defineRouting: locales, defaultLocale, localePrefix
  request.ts            # getRequestConfig: locale -> messages/<locale>.json
  navigation.ts         # locale-aware Link, redirect, usePathname sarmalayıcıları
proxy.ts                # createMiddleware(routing), localeDetection: false
messages/
  en.json
  tr.json
```

`src/app/api/contact/route.ts` (Route Handler) `[lang]` segmentinin dışında kalır, `src/app/api/` altında değişmez.

### routing.ts (as-needed)

next-intl'in resmi örnek konfigürasyonu (github.com/amannn/next-intl/.../i18n/routing.ts) referans alınır:

```ts
export const routing = defineRouting({
  locales: ["en", "tr"],
  defaultLocale: "tr",
  localePrefix: "as-needed",
});
```

### proxy.ts (localeDetection kapalı)

Next.js 16'da `middleware.ts` deprecated, dosya adı `proxy.ts` (nextjs.org/docs/messages/middleware-to-proxy). Otomatik Accept-Language yönlendirmesi bilinçli kapatılır:

```ts
export default createMiddleware({
  ...routing,
  localeDetection: false,
});
```

Dil değişimi yalnızca header'daki language-switcher'ın URL'i değiştirmesiyle olur (`src/components/layout/language-switcher.tsx`, URL tabanlı çalışacak şekilde yeniden yazılır), Accept-Language veya IP tabanlı otomatik yönlendirme yok.

### setRequestLocale + generateStaticParams

Her page ve layout'ta:

```ts
export function generateStaticParams() {
  return routing.locales.map((lang) => ({ lang }));
}

export default async function Page({ params }: { params: Promise<{ lang: string }> }) {
  const { lang } = await params;
  setRequestLocale(lang);
  // ...
}
```

Doğrulama: `next build` çıktısında tüm içerik route'ları statik (○) olmalı, yalnızca `/api/*` dynamic kalmalı.

### next/root-params Route Handler kısıtı

Next.js 16'nın root params mekanizması (`next/root-params`), `[lang]` segmentini prop drilling olmadan Server Component ağacına taşımayı sağlıyor, next-intl bunu App Router entegrasyonunda kullanıyor. Ancak bu mekanizma **Route Handler'da (`route.ts`) ve Server Action'da desteklenmiyor**. `src/app/api/contact/route.ts` bir Route Handler olduğu için locale, root params üzerinden otomatik gelmiyor; contact formundan (client component) request body'sinde veya bir header'da elle taşınmalı ve route içinde açıkça okunmalı. Bu, hata mesajlarının (jenerik hata metni, bkz. 05-backend-icerik-ve-servisler.md) doğru dilde dönebilmesi için gerekli.

### Çevirisi olmayan içerik: fallback sayfa yok

Bir proje veya blog yazısının yalnızca `content/{projects,blog}/en/<slug>.mdx` içinde karşılığı varsa ve `content/{projects,blog}/tr/<slug>.mdx` yoksa:

- `/en/projects/<slug>` route'u üretilmez (2026-08-30 sonrası şema: TR kökte, EN /en altında; eksik taraf çoğunlukla EN çevirisi) (`generateStaticParams` o dil için o slug'ı listelemez).
- `sitemap.ts` o dil için o slug'ı eklemez.
- `generateMetadata`'daki `alternates.languages` yalnızca gerçekten var olan dilleri listeler; olmayan dil için `hreflang` girdisi yazılmaz.

Bu, miaoosi.com'un uyardığı "çevrilmemiş fallback sayfaların indekslenmesi" hatasından kaçınmak için bilinçli bir kısıtlama; içerik modeli detayı 05-backend-icerik-ve-servisler.md'de.

### Blog dil politikası: TR-first

Site sahibinin ana dili Türkçe (2026-08-27 cevabı, bkz. [11-acik-sorular.md](./11-acik-sorular.md) soru 9); bu, yukarıdaki "fallback yok" politikasıyla tutarlı somut bir kural haline geliyor. Çekirdek sayfalar (Home, About, Projects case study'leri, Contact) işe alım kitlesinin uluslararası olması nedeniyle EN ve TR'de ikisi de eksiksiz ve cilalı kalır; bu sayfalarda tek dilli yayın yok. Blog ise **TR-first**: yazılar önce Türkçe yazılır, yalnızca uluslararası ilgi görecek olanlar (ör. Coolify/Traefik self-host yazısı, pentest -> fix döngüsü anlatan yazı) sonradan EN'e çevrilir. Tek dilli bir blog yazısı serbesttir ve yukarıdaki kural gereği diğer dilin `sitemap.ts` ve `alternates.languages`'ına hiç girmez; bu, mevcut fallback-yok politikasının blog'a doğrudan uygulanmasıdır, ayrı bir istisna değil. Gerekçe: Türkçe teknik DevOps/security içeriğinin yerel arama boşluğu (bkz. [08-icerik-stratejisi.md](./08-icerik-stratejisi.md) sektör bulguları).

### Eski öneksiz `/blog` yönlendirilmiyor: bilinçli kabul (2026-09-02, V-5)

`src/i18n/legacy-en-paths.ts` eski öneksiz İngilizce nav yollarını (`/about`, `/projects`, `/contact`, `/privacy`) kalıcı 308 ile `/en/...`'e taşıyor; `/blog` bu tabloda yok ve eklenmeyecek. Gerekçe: `routing.pathnames` içinde `/blog` iki dilde de aynı yol, yani öneksiz `/blog` TR-varsayılan geçişinden sonra Türkçe listenin kanonik adresi. Tabloya eklenmesi kanonik Türkçe sayfayı kendi adresinden sürer, `alternates.canonical` ile çelişir ve TR listesini erişilemez yapar.

Sonuç, aynı sınıftan iki farklı davranış: `/projects` 308 ile `/en/projects`'e giderken `/blog` sessizce Türkçe listeyi sunuyor. Bu kabul ediliyor, çünkü tek gerçek alternatif (Türkçe listeyi `/yazilar` gibi ayrı bir yola taşımak) yeni bir URL kümesi ve yeni bir yönlendirme borcu üretirdi. Detay sayfaları da aynı mantıkta: `/blog/<slug>` ve `/projects/<slug>` iki dilde aynı slug'ı kullandığı için öneksiz adres Türkçe kanoniktir. İngilizce okuyucunun giriş noktası `/en/blog`; geçişi sayfadaki dil değiştirici, `hreflang` kümesi ve sitemap taşıyor.

Yalnızca İngilizce çevirisi olan bir yazı için öneksiz `/blog/<slug>`'ı `/en/blog/<slug>`'a yönlendirmek de değerlendirildi ve yapılmadı: `[lang]` layout'u `dynamicParams = false` ile çalışıyor, yani `generateStaticParams`'ın üretmediği slug sayfa koduna hiç ulaşmadan 404 oluyor. Yönlendirme için o slug'ı Türkçe tarafta da üretmek gerekirdi, bu da "çevrilmemiş fallback sayfa üretilmez" politikasını ve `scripts/assert-static-routes.mjs`'in "prerender edilen rota kümesi içerik slug'larına eşittir" kuralını bozardı. Bugün tek dilli bir yazı da yok, yani kural ölü kod olarak eklenmiş olurdu.

### Türkçe'ye özel notlar

- `toLocaleUpperCase("tr-TR")` / `toLocaleLowerCase("tr-TR")`: her harf büyütme/küçültme işleminde zorunlu (yukarıda, Riskler bölümünde detay).
- **latin-ext font subset**: TR karakterleri (İ, ı, ş, ğ, ç, ö, ü) Latin Extended-A bloğunda; font vendoring kararında (Geist Sans/Mono + Instrument Serif, `next/font/local`) latin-ext alt kümesi de indirilmeli, yoksa TR sayfalarda bu karakterler fallback fonta düşer. Detay ve dosya yolları: 03-tasarim-ui-ux.md.
- `<html lang="...">`: cookie yerine `[lang]` route param'ından üretilir, `en` veya `tr` olarak layout'ta set edilir.

## İlgili Dokümanlar

- [02-stack-karari.md](./02-stack-karari.md) - Next.js 16.3.3 + next-intl 4.13.7 genel stack kararı ve gerekçesi
- [03-tasarim-ui-ux.md](./03-tasarim-ui-ux.md) - font vendoring, latin-ext alt kümesi, dosya yolları
- [05-backend-icerik-ve-servisler.md](./05-backend-icerik-ve-servisler.md) - Velite içerik modeli, per-locale MDX yapısı, fallback yok politikasının uygulanışı, contact route'unda locale taşıma
- [07-seo-ve-metadata.md](./07-seo-ve-metadata.md) - alternates.canonical/languages, x-default, sitemap.ts, hreflang doğrulama süreci
- [10-yol-haritasi.md](./10-yol-haritasi.md) - Faz 2 (i18n yeniden mimarisi) sıralaması ve doğrulama adımı
- [11-acik-sorular.md](./11-acik-sorular.md) - Soru 9: blog dil politikası (cevaplandı: TR-first, çekirdek sayfalar EN+TR eksiksiz)

## Kaynaklar

- https://next-intl.dev/docs/routing/setup
- https://next-intl.dev/docs/routing/middleware
- https://next-intl.dev/docs/getting-started/app-router
- https://github.com/amannn/next-intl/blob/main/examples/example-app-router-playground/src/i18n/routing.ts
- https://github.com/amannn/next-intl/blob/main/examples/example-app-router/src/proxy.ts
- https://github.com/amannn/next-intl/blob/main/docs/src/pages/blog/nextjs-root-params.mdx
- https://github.com/amannn/next-intl/discussions/355
- https://nextjs.org/docs/messages/middleware-to-proxy
- https://nextjs.org/docs/app/api-reference/file-conventions/proxy
- https://paraglidejs.com/next-js
- https://github.com/opral/paraglide-js
- https://developers.google.com/search/docs/specialty/international/localized-versions
- https://developers.google.com/search/docs/specialty/international/managing-multi-regional-sites
- https://www.dchost.com/blog/en/hreflang-done-right-the-calm-guide-to-cctlds-subdirectories-subdomains-and-x-default/
- https://merj.com/blog/your-accept-language-redirects-could-be-blocking-search-engines-and-ai-crawlers
- https://support.google.com/webmasters/thread/151197680/why-avoid-automatic-redirection-when-hreflang-exists?hl=en
- https://miaoosi.com/en/writing/nextjs-i18n-seo-observability
- https://en.wikipedia.org/wiki/Dotted_and_dotless_I_in_computing
- https://www.npmjs.com/package/next-intl
- https://www.npmjs.com/package/next-international
- https://www.npmjs.com/package/@inlang/paraglide-js
- https://www.npmjs.com/package/@inlang/paraglide-next
- https://www.npmjs.com/package/next-i18next
